matplotlib 函数手册
星猫译
函数介绍
acorr Plot the autocorrelation of x.绘制 X 的自相关
annotate
Create an annotation: a piece of text referring to a data
point.
用箭头在指定的一个数据点创建一个注释或一段文本。
arrow Add an arrow to the axes.为axes 添加一个箭头。
autoscale
Autoscale the axis view to the data (toggle).自动缩放轴视图的
数据（切换）。
axes Add an axes to the figure.为当前figure 添加一个axes。
axhline
Add a horizontal line across the axis.添加一条穿越axis 的水平
线。
axhspan
Add a horizontal span (rectangle) across the axis.添加一条穿
越axis 的水平矩形。
axis
Convenience method to get or set axis properties.获取或设置轴
属性的便捷方法。
axvline Add a vertical line across the axes.添加一条穿越axes 的垂线。
axvspan
Add a vertical span (rectangle) across the axes.添加一个与
axes 交叉的垂直跨度（矩形）。
bar Make a bar plot.做一个垂直条形图。
barbs Plot a 2-D field of barbs.绘制一个倒钩的二维场（风场）。
barh Make a horizontal bar plot.做一个横向条形图。
box Turn the axes box on or off.打开或关闭主轴箱。
boxplot Make a box and whisker plot.使一盒须图。
broken_barh Plot horizontal bars.绘制水平杆。
cla Clear the current axes.清除当前axes。
clabel
Label a contour plot.
为等值线图设标签
clf Clear the current figure.清除当前figure。
clim
Set the color limits of the current image.设置当前图像的颜色
取值范围。
close Close a figure window.关闭figure 窗口。
cohere
Plot the coherence between x and y.绘制 X 和Y 之间的相关性分
析
colorbar Add a colorbar to a plot.为图添加彩条标值。
contour Plot contours.绘制等值线。
contourf Plot contours.绘制填充等值线。
csd Plot cross-spectral density.绘制交叉谱密度。
delaxes
Remove an axes from the current figure.从目前的figure 删除
axes。
draw Redraw the current figure.再次绘制当前figure。
errorbar Plot an errorbar graph.绘制errorbar 图。
eventplot
Plot identical parallel lines at specific positions.在特定的
位置绘制相同的平行线。
figimage
Adds a non-resampled image to the figure.为figure 添加一个非
重采样图像。
figlegend Place a legend in the figure.为figure 放置一个标注（legend）
figtext Add text to figure.为figure 添加文字。
figure Creates a new figure.创建一个新的figure。
fill Plot filled polygons.绘制填充多边形。
fill_between Make filled polygons between two curves.使两曲线间填充色彩。
fill_betweenx
Make filled polygons between two horizontal curves.使两水平线
间填充色彩。
findobj Find artist objects.发现artist 对象。
gca
Return the current axis instance.返回当前轴实例（axis
instance）。
gcf
Return a reference to the current figure.返回当前figure 的序
号。
gci Get the current colorable artist.获取当前彩条的artist。
get_figlabels
Return a list of existing figure labels.返回一个当前figure 的
标签列表。
get_fignums
Return a list of existing figure numbers.返回一系列figure 的
序号。
grid(on/off) Turn the axes grids on or off.打开或关闭坐标网格。
hexbin Make a hexagonal binning plot.做一个六边形箱图。
hist Plot a histogram.绘制直方图。
hist2d Make a 2D histogram plot.使一个二维直方图。
hlines Plot horizontal lines.绘制水平线。
hold Set the hold state.设置hold 状态。
imread
Read an image from a file into an array.读取一个图像，从图形
文件中提取数组。
imsave Save an array as in image file.保存为图像文件中的数组。
imshow Display an image on the axes.在axes 上显示图像。
ioff Turn interactive mode off. 关闭互动模式。
ion Turn interactive mode on.开启互动模式。
ishold
Return the hold status of the current axes.返回当前axes 的
hold 状态。
isinteractive Return status of interactive mode.返回互动模式状态。
legend Place a legend on the current axes.为当前axes 放置标注。
locator_params Control behavior of tick locators.控制轴刻度标签
loglog
Make a plot with log scaling on both the x and y axis.使X、
Y 轴为log 刻度。
margins Set or retrieve autoscaling margins.设置或检索自动缩放功能。
matshow
Display an array as a matrix in a new figure window.在新
figure 窗口显示数组矩阵
minorticks_off
Remove minor ticks from the current plot.移除当前数轴上的次刻
度
minorticks_on
Display minor ticks on the current plot.显示当前数轴上的次刻
度
over
Call a function with hold(True).调用一个函数，并且hold 为
（True）。
pause Pause for interval seconds.Pause 的时间间隔（秒）。
pcolor
Create a pseudocolor plot of a 2-D array.创建一个二维阵列的伪
彩色图。
pcolormesh Plot a quadrilateral mesh.绘制一个四边形网格。
pie Plot a pie chart.绘制一个饼图。
plot Plot lines and/or markers to the Axes.绘制当前axes。
plot_date Plot with data with dates.绘制数据日期。
plotfile Plot the data in in a file.将图绘入文件
polar Make a polar plot.绘制极坐标图。
psd Plot the power spectral density.绘制功率谱密度图。
quiver Plot a 2-D field of arrows.绘制二维箭头图（风矢量图）。
quiverkey Add a key to a quiver plot.为风矢量图绘制单位标量
rc Set the current rc params.设置当前的RC 参数。
rc_context
Return a context manager for managing rc settings.返回一个用
于管理RC 设置上下文管理器。
rcdefaults Restore the default rc params.恢复默认的RC 参数。
rgrids
Get or set the radial gridlines on a polar plot.获取或设置径
向网格在极坐标图。
savefig Save the current figure.保存当前图。
sca
Set the current Axes instance to ax.为ax 设置当前（axes）轴实
例。
scatter
Make a scatter plot of x vs y, where x and y are sequence
like objects of the same lengths.做一个X 和Y 的散点图，其中
X 和Y 是相同长度的序列的对象。
sci Set the current image.设置当前的图像。
semilogx Make a plot with log scaling on the x axis.使x 轴为log 刻度。
semilogy Make a plot with log scaling on the y axis.使y 轴为log 刻度。
set_cmap Set the default colormap.设置默认的colormap。
setp Set a property on an artist object.对artist 对象设置属性。
show Display a figure.显示图。
specgram Plot a spectrogram.绘制谱图。
spy
Plot the sparsity pattern on a 2-D array.绘制一个二维阵列的稀
疏模式。
stackplot Draws a stacked area plot.绘制一个堆叠面积图。
stem Create a stem plot.创造一个stem 图。
step Make a step plot.做一个步阶图
streamplot Draws streamlines of a vector flow.绘制一个流场图。
subplot
Return a subplot axes positioned by the given grid
definition.返回一个subplot axes
subplot2grid Create a subplot in a grid.在网格中创建一个subplot。
subplot_tool
Launch a subplot tool window for a figure.获取subplot 工具窗
口。
subplots
Create a figure with a set of subplots already
made.一个图中包含多个子图
subplots_adjust Tune the subplot layout.调整subplot 布局
suptitle Add a centered title to the figure.在图添加一个中心标题。
switch_backend Switch the default backend.交换机默认的后台。
table Add a table to the current axes.当前axes 添加table。
text Add text to the axes.在轴上添加文本。
thetagrids
Get or set the theta locations of the gridlines in a polar
plot.设置极坐标网格线θ 位置。
tick_params
Change the appearance of ticks and tick labels.改变刻度及刻度
标签外观
ticklabel_format
Change the ScalarFormatter used by default for linear axes.通
过使用默认线性axes 轴更改 scalarformatter 。
tight_layout
Automatically adjust subplot parameters to give specified
padding.自动调节subplot 参数进行指定填充。
title Set a title of the current axes.设置当前axes 标题。
tricontour
Draw contours on an unstructured triangular grid.在非结构三角
形网格绘制等值线。
tricontourf
Draw contours on an unstructured triangular grid.在非结构三角
形网格绘制填充等值线。
tripcolor
Create a pseudocolor plot of an unstructured triangular
grid.创建一个非结构三角形网格伪彩色图。
triplot
Draw a unstructured triangular grid as lines and/or
markers.画一个非结构三角形网格图（类似plot 函数）。
twinx
Make a second axes that shares the x-axis.制作第二个axes，并
共用x 轴。
twiny
Make a second axes that shares the y-axis.制作第二个axes，并
共用y 轴。
vlines Plot vertical lines.绘制垂直线。
xcorr
Plot the cross correlation between x and y.绘制x 和y 的相关性
分析图
xkcd
Turns on xkcd sketch-style drawing mode.开启XKCD 草图风格绘
画模。
xlabel
Set the x axis label of the current axis.在当前轴设置 X 轴的
标签。
xlim
Get or set the x limits of the current axes.设置当前axes 的
x 轴取值范围
xscale Set the scaling of the x-axis.设置 X 轴缩放。
xticks
Get or set the x-limits of the current tick locations and
labels.设置当前x 轴刻度位置的标签和值。
ylabel
Set the y axis label of the current axis.设置当前轴 y 轴标签。
ylim
Get or set the y-limits of the current axes.设置当前axes 的
y 轴取值范围
yscale Set the scaling of the y-axis.设置 Y 轴缩放。
yticks
Get or set the y-limits of the current tick locations and
labels.设置当前y 轴刻度位置的标签和值。
函数：
1. matplotlib.pyplot.acorr(x, hold=None, **kwargs)
自相关函数
acorr(x, normed=True, detrend=mlab.detrend_none, usevlines=True,
maxlags=10, **kwargs) #x 的自相关
如果normed = True,通过零滞后自相关规范数据，x 通过非趋势可调用（默认没有归
一化）的方式除趋势
数据绘制形如plot(lags, c, **kwargs)
返回值是元组(lags, c, line)，其中：
￥lags 是一个长度为2×maxlags + 1 的滞后（lags）向量
￥C 是2×maxlags + 1 的自相关向量
￥line 是一个line2d 实例，通过plot()返回
默认linestyle 是None，默认的marker 是'o'，交叉相关性是通过numpy.correlate()函
数、mode = 2 实现的。
若usevlines 是True, vlines()将被调用（而不调用plot()函数），用来绘制从起点
到acorr 的垂线。否则，plot（）由Line2D properties 属性参数（kwargs）决定。
maxlags 是正整数，决定lags 的显示数目。默认值None 将返回（2×len（x）- 1）
个lags，返回值是一个元组(lags, c, linecol, b)，其中：
linecol 是linecollection
b 是x-axis
其他参数参见 Line2D的kwargs 属性
2 . matplotlib.pyplot.annotate(*args, **kwargs)
创建一个文本注释：从指定点指向目标点
annotate('注释内容', xy,
xytext=None,xycoords='data',textcoords='data', arrowprops=None,
**kwargs)
xy：被注释点的位置，xytext：注释文本的位置坐标
xycoords 和textcoords 是字符串，指示xy 和xytext 的坐标关系：
Property Description
‘figure points’
points from the lower left corner of the
figure
起点从figure 左下角
‘figure pixels’
pixels from the lower left corner of the
figure
pixels（像素）从figure 左
下角
‘figure fraction’
0,0 is lower left of figure and 1,1 is upper
right
（0，0）是figure 的左下
方，（1，1）是右上方
‘axes points’ points from lower left corner of axes 起点从axes 的左下方
‘axes pixels’ pixels from lower left corner of axes
pixels（像素）从axes 左
下角
‘axes fraction’
0,0 is lower left of axes and 1,1 is upper
right
（0，0）是axes 的左下方，
（1，1）是右上方
‘data’
use the coordinate system of the object
being annotated (default)
使用被注释对象的坐标系
统
‘offset points’
Specify an offset (in points) from the xy
value
从xy 点指定一个偏移量
‘polar’
you can specify theta, r for the
annotation, even in cartesian plots. Note
that if you are using a polar axes, you do
not need to specify polar for the
coordinate system since that is the native
“data” coordinate system.
即使在直角坐标系，也可
以指定（θ，r)的值。如果
使用极坐标，不需要指定
极点
通用箭头属性：
Key Description
width the width of the arrow in points 箭头宽度
frac
the fraction of the arrow length occupied
by the head
箭头尖端（头部）所占比
例
headwidth
the width of the base of the arrow head in
points
箭头尖端的尾部宽度
shrink
oftentimes it is convenient to have the
arrowtip and base a bit away from the
text and point being annotated. If d is the
distance between the text and annotated
point, shrink will shorten the arrow so
the tip and base are shink percent of the
distance d away from the endpoints. ie,
shrink=0.05 is 5%
缩短箭头，以容纳文字。
Shrink=0.05 表示缩短5%
?kwargs any key for matplotlib.patches.polygon
通用绘图参数，包括
facecolor、alpha 等，可查
询kwargs 表格。
花式箭头参数：
Key Description
arrowstyle the arrow style 箭头风格
connectionstyle the connection style
relpos default is (0.5, 0.5) 默认（0.5，0.5）
patchA default is bounding box of the text 默认边界框
patchB default is None 默认无
shrinkA default is 2 points 默认2
shrinkB default is 2 points 默认2
mutation_scale default is text size (in points) 默认为字体大小
mutation_aspect default is 1. 默认为1
? any key for matplotlib.patches.PathPatch
其他参数参见kwargs
3. matplotlib.pyplot.arrow(x, y, dx, dy, hold=None, **kwargs)
为axes 添加箭头
arrow(x, y, dx, dy, **kwargs)
绘制指定的箭头从（X，Y）指向（X + Y + dx，dy）。常用的参数：
width（箭头尾部宽度）: 浮点数 (默认: 0.001)
length_includes_head（length 值
是否包含head[箭头尖部]）:
[True | False] (默认: False)
head_width（箭头head 宽度）: float（浮点数）或None (默认: 3*width)
head_length: float（浮点数）或None (默认: 1.5 * head_width)
shape（箭头形状）:
[‘full’('完整的'), ‘left’('左半部分'), ‘right’('右半部分')]
(default: ‘full’)
overhang（箭头尖端形状）: float（浮点数） (默认: 0)可以是负值，也可以大于1
head_starts_at_zero:
[True | False] (默认: False)如果为True，箭头尖部起
点在坐标0 点，而False 则终点在坐标0 点
其他参数参见 matplotlib.patches.Polygon 实例。
4. matplotlib.pyplot.autoscale(enable=True, axis='both', tight=None)
自动缩放轴视图的数据（切换），方便简单的轴视图自动缩放的方法。
enable: [True | False | None]
True（默认）开启自动缩放，False 关闭，
None 离开（自动缩放状态不改变）
axis: [‘x’ | ‘y’ | ‘both’] 选择缩放的轴线，默认'both'
tight: [True | False | None]
如果True，根据data 范围设置视角
（view）范围；若为False，可扩大视角范
围；若为None，只有一个data 时，按
True 处理，其他按False 对待
返回None
5. matplotlib.pyplot.autumn()
对autumn()函数设置默认的colormap，并应用于当前图像。更多信息查询
help(colormaps)。
6. matplotlib.pyplot.axes(*args, **kwargs)
对figure 增加一个axes
由rect 指定axes 添加的位置：
axes() 创建一个默认的完整的subplot（111）图轴。
axes(rect, axisbg='w')
其中rect = [left, bottom, width, height]（四个参数取值范
围为[0,1]）axisbg 指背景颜色，默认为white
axes(h) 其中h 指axes 实例，包含下列参数：
kwarg Accepts Description
axisbg color Axes 图轴背景色
frameon [True|False] 是否绘制图像框架
sharex otherax 当前axes 图轴是否与其它图轴共用x 轴
sharey otherax 当前axes 图轴是否与其它图轴共用y 轴
polar [True|False] 使用极坐标轴?
7.matplotlib.pyplot.axhline(y=0, xmin=0, xmax=1, hold=None, **kwargs)
添加一条穿越axis 的水平线。
axhline(y=0, xmin=0, xmax=1, **kwargs)
绘制一条从xmin 到xmax 的水平线，其中0=left, 0.5=middle, 1.0=right，而水平线的长度视
水平轴长度而定。
该函数返回值为 Line2D（二维曲线）参量，详情查询 Line2D属性。
8.matplotlib.pyplot.axhspan(ymin, ymax, xmin=0, xmax=1, hold=None, **k
wargs)
添加一条穿越axis 的水平span（矩形）。
其中x 轴方向： 0=left, 0.5=middle, 1.0=right，水平线的长度视水平轴长度而定。而y 轴则
根据坐标点确定。
返回值为 matplotlib.patches.Polygon实例，详情查询matplotlib.patches.Polygon
9. matplotlib.pyplot.axis(*v, **kwargs)
获取或设置轴属性的便捷方法。没有输入调用变量:
>>> axis() #返回当前axes 范围（limits） [xmin, xmax, ymin, ymax].
……………………………………………………………………………………………………
通过输入v = [xmin, xmax, ymin, ymax]. 设置x、y 的取值范围
>>> axis(v)
……………………………………………………………………………………………………
>>> axis('off') #关闭轴线极其标签
……………………………………………………………………………………………………
>>> axis('equal') #使x、y 轴长度一致
……………………………………………………………………………………………………
>>> axis('scaled') #调整图框的尺寸（而不是改变坐标轴取值范围），使x、y 轴
长度一致
……………………………………………………………………………………………………
>>> axis('tight') #改变X 和Y 轴的限制，使所有数据被展示。如果所有的数据
已经显示，它将移动到图形的中心而不修改（XMAX - XMIN）或（YMAX - Ymin）。
……………………………………………………………………………………………………
>>> axis('image') #缩放axis 范围（limits），等同于对data 缩放范围
……………………………………………………………………………………………………
>>> axis('auto')
>>> axis('normal')
不推荐使用。他们恢复默认状态；轴限的自动缩放以使数据合适地在图中。
10.matplotlib.pyplot.axvline(x=0, ymin=0, ymax=1, hold=None, **kwargs)
添加一条穿越axes 的垂线。
axvline(x=0, ymin=0, ymax=1, **kwargs)
绘制一条从ymin 到ymax 的水平线，其中0=bottom, 0.5=middle, 1.0=top，而垂线的长度视
水平轴长度而定。
该函数返回值为 Line2D（二维曲线）参量，与 Line2D属性一致：
11.matplotlib.pyplot.axvspan(xmin, xmax, ymin=0, ymax=1, hold=None, *
*kwargs)
添加一个与axes 交叉的垂直跨度（矩形）。
axvspan(xmin, xmax, ymin=0, ymax=1, **kwargs)
其中x 轴方向： 0=bottom, 0.5=middle, 1.0=top，垂线的长度视水平轴长度而定。
而x 轴则根据坐标点确定。
返回值为 matplotlib.patches.Polygon实例：
12.matplotlib.pyplot.bar(left, height, width=0.8, bottom=None, hold=None,
**kwargs)
绘制条形图
矩形边界条形图：left, left + width, bottom, bottom + height(left, right, bottom
and top edges)
left : 标量或数组bar 左端的x 轴坐标
height : 标量或数组bar 高度
width : 标量或数组bar 宽度
bottom : 标量或数组，可选，默认为None bar 的底部高度
color : 色彩、标量或数组，可选bar 内部颜色
edgecolor : 色彩、标量或数组，可选bar 边缘颜色
linewidth : 标量或数组，可选，默认为None
bar 边缘宽度，若为None，取默认
宽度；若为0，则无边缘
xerr : 标量或数组，可选，默认为None 若不为None，将生成errorbar(s)
yerr : 标量或数组，可选，默认为None 若不为None，将生成errorbar(s)
ecolor : 标量或数组，可选，默认为None 指定errorbar(s)的颜色
capsize : 整数，可选，默认值：3
指定errorbar(s)的cap（帽子）的长
度
error_kw : 字典
errorbar 字典，ecolor 和capsize 可
在此指定，而不作为独立参量
align : [‘edge’ | ‘center’],可选, 默认: ‘edge’
若为‘edge’,对于竖条形图，按bar 左
边缘排列；对于横条形图，按bar 下
边缘排列。若为'center'，把左端的
参数作为bar 的中心坐标。
orientation :
‘vertical’ | ‘horizontal’,可选, 默认:
‘vertical’
选择竖条形图、横条形图
log :
True、False（0、1），可选，默
认为False
如果为True，为对数轴
返回:class（类别）:`matplotlib.patches.Rectangle`实例。
其他可选参量，参见matplotlib.patches.Polygon 实例（完全一致）。
13. matplotlib.pyplot.barbs(*args, **kw)
绘制二维风向杆
barb(X, Y, U, V, C, **kw)
X, Y: 风向杆的坐标位置，默认为barb 的头部
U, V: 风向的x、y 方向分量
C: 风向杆颜色
length: 风向杆长度，默认为9
pivot（支点）: [ ‘tip’ | ‘middle’ ]默认‘tip’ 风向杆围绕着指定点旋转
barbcolor: [ color | color 序列、数组 ] 指定除了flags 部分之外的颜色，会覆
盖facecolor
flagcolor [ color | color 序列、数组 ] 指定flags 颜色，会覆盖facecolor
sizes:（该参量为
字典）
‘spacing’ - flags 间距
‘height’ - 高度 (shaft 到top 距离)
‘width’ - flag 宽度, full barb（完整barb）的两倍宽度
‘emptybarb’ - radius of the circle used for low magnitudes 用于低幅度
的圆半径
fill_empty:
A flag on whether the empty barbs (circles) that are drawn should be
filled with the flag color. If they are not filled, they will be drawn such
that no color is applied to the center. Default is False
rounding:
A flag to indicate whether the vector magnitude should be rounded
when allocating barb components. If True, the magnitude is rounded
to the nearest multiple of the half-barb increment. If False, the
magnitude is simply truncated to the next lowest multiple. Default is
True
barb_increments:
（该参量为字典）
‘half’ - half barbs (Default is 5)
‘full’ - full barbs (Default is 10)
‘flag’ - flags (default is 50)
flip_barb:
Either a single boolean flag or an array of booleans. Single boolean
indicates whether the lines and flags should point opposite to normal
for all barbs. An array (which should be the same size as the other data
arrays) indicates whether to flip for each individual barb. Normal
behavior is for the barbs and lines to point right (comes from wind
barbs having these features point towards low pressure in the
Northern Hemisphere.) Default is False
额外参量参见PolyCollection
14.matplotlib.pyplot.barh(bottom, width, height=0.8, left=None, hold=Non
e, **kwargs)
绘制横条形图
barh(bottom, width, height=0.8, left=0, **kwargs)#
返回:class（类别）:`matplotlib.patches.Rectangle`实例。必要参量：
bottom Bars 的垂直位置的底部边缘
width Bars 的长度
可选参量：
Keyword 描述
height bars 的高度
left bars 左边缘x 轴坐标值
color bars 颜色
edgecolor bars 边缘颜色
linewidth bar 边缘宽度；None 表示默认宽度；0 表示不i 绘制边缘
xerr 若不为None，将在bar 图上生成errobars
yerr 若不为None，将在bar 图上生成errobars
ecolor 指定errorbar 颜色
capsize 指定errorbar 的顶部（cap）长度
align
‘edge’ (默认) | ‘center’：‘edge’以底部为准对齐；
‘center’以y 轴作为中心？
log [False|True] False (默认)，若为True，使用log 坐标
其他参数参见 matplotlib.patches.Polygon 实例
15. matplotlib.pyplot.box(on=None)
设置axes 边框是否打开，[‘on’ 或 ‘off’],也可以是布尔数组
16.matplotlib.pyplot.boxplot(x, notch=False, sym='b+', vert=True, whis=
1.5, positions=None, widths=None, patch_artist=False,bootstrap=None, user
medians=None, conf_intervals=None, hold=None)
绘制方框和线
boxplot(x, notch=False, sym='+', vert=True, whis=1.5, positions=None,
widths=None, patch_artist=False,bootstrap=None, usermedians=None,
conf_intervals=None)
x : 表示向量的数组或列表
notch : [ False (默认) | True ]
若为False（默认），绘制一个
矩形框。若为True，生成一个
有缺口的矩形框
sym : [ 默认‘b+’ ]
flier points（飞点）的点符号，
若不显示飞点，输入空字符串
('')
vert : [ False | True (默认) ]
若为True（默认），使box 竖
放。若为False，使横框。
whis : [ default 1.5 ]
定义whisker（box 的延伸线）
范围？具体参阅说明
bootstrap : [ None (默认) | integer（整数） ] 具体参阅说明
usermedians : [ 默认None ] 具体参阅说明
conf_intervals : [ 默认None ] 具体参阅说明
positions : [默认 1,2,...,n ]
设置水平位置，刻度和取值范围
自动匹配
widths : [默认0.5 ]
设置每个box 的宽度，通过输入
一个标量或矢量。默认值
0.5 或0.15*(两端距离)
patch_artist : [ False (默认) | True ]
False 对应 Line2D 属性，
True 对应Patch 属性
返回值包含参量：
boxes:
the main body of the boxplot showing the quartiles and the median’s
confidence intervals if enabled.
medians: horizonal lines at the median of each box.
whiskers: the vertical lines extending to the most extreme, n-outlier data points.
caps: the horizontal lines at the ends of the whiskers.
fliers: points representing data that extend beyone the whiskers (outliers).
17.matplotlib.pyplot.broken_barh(xranges, yrange, hold=None, **kwargs)
绘制水平方框
broken_barh(self, xranges, yrange, **kwargs)
必要参量：
xranges (xmin, xwidth)序列
yrange (ymin, ywidth)序列
其他参数参考matplotlib.collections.BrokenBarHCollection 属性。
18. matplotlib.pyplot.cla()
清除当前axes（图轴）
19. matplotlib.pyplot.clabel(CS, *args, **kwargs)
为等值线图添加标签
clabel(cs, v, **kwargs)
cs 是等值线函数（contour）返回的ContourSet 目标，而v 包含label 标签信息。
可选参量：
fontsize:字体大小用标量表示，或名称表示‘smaller’, ‘x-large’
colors:
若为None，每个标签的颜色相匹配对应的轮廓颜色；若为一种颜
色，如colors=‘r’或colors=‘red’，则所以标签显示这种颜色；若
为一系列颜色（色彩字符串或数组），则不同label 按顺序绘制不
同色彩
inline: [True(默认),False]表示标签下面的等值线是否被并且覆盖
inline_spacing: 默认为5，值越大，标签位置越靠近等值线较平直的部位
fmt: 指定标签格式的字符串，默认： ‘%1.3f’。更多信息参见手册
manual: 若为True，可用鼠标或键盘确定标签位置
rightside_up: 若为True（默认），标签将竖直放置
use_clabeltext:
默认False。若为True，等值线变化时，标签可随等值线的变化而
变化
20. matplotlib.pyplot.clf()
清除当前figure
21.matplotlib.pyplot.clim(vmin=None, vmax=None)
设置当前image 的色彩取值范围
22. matplotlib.pyplot.close(*args)
关闭figure 窗口
close() 关闭当前窗口
close(h) 其中h 指figure 实例，关闭那个figure
close(num) 关闭第num 个figure
close(name) 关闭名为name 的figure
close('all') 关闭所有figure
23.matplotlib.pyplot.cohere(x, y, NFFT=256, Fs=2, Fc=0, detrend=<functi
on detrend_none at 0x2635de8>, window=<function window_hanning at
0x2635b90>, noverlap=0, pad_to=None, sides='default', scale_by_freq=Non
e, hold=None, **kwargs)
绘制x 与y 之间的相关性
cohere(x, y, NFFT=256, Fs=2, Fc=0, detrend = mlab.detrend_none, window =
mlab.window_hanning, noverlap=0, pad_to=None, sides='default',
scale_by_freq=None, **kwargs)
相关性函数，关键参数：
NFFT: integer
每排的FFT 数据点数。必须均匀，2 的平方最有
效，默认值256。不能用于获取零填充（补零），
否则结果错误， 使用pad_to 替代。
Fs: scalar 标量
采样频率（样品每时间单位[time unit]）。它是
用来计算傅立叶频率,freqs,周期中每时间单位(in
cycles per time unit)。默认值为2
detrend: callable？
应用在每个fft 之前的函数，旨在消除平均或线性
趋势。不像在MATLAB，detrend 参数是一个向
量，而在matplotlib 是一个函数。Pylab 模块定
义了detrend_none(), detrend_mean(), and
detrend_linear()函数，也可以用户自定义。
window:
callable 或
ndarray（pytho
n 的某种数组）
一个函数或向量的傅立叶变换长度。创建窗口向
量出现window_hanning()，window_none()，
NumPy.blackman()，NumPy.hamming()，
NumPy.bartlett()，SciPy.signal()，
scipy.signal.get_window(),等。默认的是
window_hanning()，如果一个函数作为参数传递，
它必须以一个数据段作为参数和返回段的窗口版
本。
pad_to: integer
执行FFT 时，数据段垫点的个数。不同于
NFFT 指定使用的数据点的数目。在不增加
PSD 的实际分辨率（分辨峰之间的最小距离）的
情况下，可以在图中获取更多的点。这对应于调
用fft()中的n 参数，默认为None，可设置
pad_to 使等同于NFFT。
sides:
[ ‘default’ |
‘onesided’ |
‘twosided’ ]
指定返回哪一侧的PSD，默认‘default’，它返回
one-sided 真实数据和复杂的数据。'one-sided'返
回一侧， ‘twosided’返回两侧
scale_by_freq: boolean 布尔数
指定是否返回由缩放频率缩放的密度值，单位
Hz^-1。这允许集成在返回的频率值。
MATLAB 兼容默认是True 的。
noverlap: integer 重叠的块之间的点的数量。默认值是0（无重叠）
。
Fc: integer
X 的中心频率（缺省为0）。offsets the x extents
of the plot to reflect the frequency range used
when a signal is acquired and then filtered and
downsampled to baseband.
返回元组(Cxy, f)，f 是相关性向量的频率。
其他参数参见 Line2D属性。
24.matplotlib.pyplot.colorbar(mappable=None, cax=None, ax=None, **kw)
对一张plot 添加一个彩条（colorbar ）
colorbar(mappable, ax=ax, **kwargs)
mappable
应用于该colorbar 的参量，包括Image, ContourSet 等。该参量对于
colorbar()方法是必须的，但对该函数是可选的，默认当前image。
cax None | 该colorbar 将被绘制的axes 目标
ax None（默认）|与axes 相关联的colorbar
use_gridspec
False （默认）|若cax=None，将创建一个新的axes 实例；若ax 是一
个Subplot 实例、并且use_gridspec=True，将创建一个Subplot 使用
的grid_spec 模块实例，提供给cax
axes 属性：
Property Description
orientation vertical 或horizontal（垂直或水平）
fraction 0.15; colorbar 所占axes 的百分比
pad 0.05（vertical 垂直）, 0.15（horizontal 水平）; 两个axes 的间距
shrink 1.0; colorbar 收缩比例
aspect 20; ratio of long to short dimensions（长宽比？）
anchor
(0.0, 0.5)[vertical 垂直]; (0.5, 1.0)[horizontal 水平];colorbar 在axes 的位
置
panchor
(1.0, 0.5)[vertical 垂直]; (0.5, 0.0)[horizontal 水平];colorbar 的源
axes(parent axes)所在位置。若为False, 源axes 的位置不会改变
colorbar 属性：
Property Description
extend
[ ‘neither’ | ‘both’ | ‘min’ |
‘max’ ]
若不是‘neither’，将不显示colorbar 的尖
端。该参数也可通过set_under 和
set_over 方法设置
extendfrac
[ None | ‘auto’ | length |
lengths ]
若为None,两端的三角尖端长度占
colorbar 长度的5%（默认设置）；若为
‘auto’，三角尖端长度与colorbar 平直部
分长度一样（当spacing[间距]设置为
‘proportional’）；若为一个标量，表征
三角尖端长度占colorbar 平直部分长度
比例；若为[a,b](两个元素序列)，表征两
个三角尖端分别占colorbar 平直部分长
度比例。
extendrect [ False | True ]
若为False，colorbar 将去掉平直部分，
只剩三角尖端；若为True，将去掉三角
尖端，只剩colorbar 平直部分
spacing [ ‘uniform’ | ‘proportional’ ]
‘uniform’使colorbar 的每个色彩均匀间
隔，‘proportional’则根据data（数据）
指定间隔
ticks
[ None | list of ticks | Locator
object ]
若为None，刻度将根据导入的数据自动
分配
format
[ None | format string |
Formatter object ]
若为None，采用ScalarFormatter；也可
以输入格式字符串，如：‘%.3f’；也可以
是Formatter object
drawedges [ False | True ] 是否绘制彩条边界
以下可能仅在索引color 的情况下使用（当norm=NoNorm()或其他非同寻常的情况下）：
boundaries None 或者sequence（一个序列）
values
None 或者sequence（一个序列，该序列的值低于
boundaries 的值）
25. matplotlib.pyplot.colors()
通用颜色：
Alias Color
‘b’ blue
‘g’ green
‘r’ red
‘c’ cyan
‘m’ magenta
‘y’ yellow
‘k’ black
‘w’ white
其他色彩可通过html 十六进制字符串指定，或者html 颜色名称，如：
color = '#eeefff'
或者color = 'burlywood'
color = 'chartreuse'
也可以通过R,G,B 元组，其中每个R,G,B 取值范围[0,1]
26.matplotlib.pyplot.contour(*args, **kwargs)
绘制等值线
contour(X,Y,Z,N) #N 表示等值线数量
可选重要参量：
colors:
[ None | string |
(mpl_colors) ]
若为None，颜色分配（colormap）由
cmap 指定；若为一个色彩字符串，如
colors=‘r’或colors=‘red’，则所以标签显
示这种颜色；若为一系列颜色（色彩字符
串或数组），则不同等值线按顺序绘制不
同色彩
alpha: 0-1 透明度
cmap: [ None | Colormap ]
一个colormap 实例，若cmap=None 且
colors=None，则使用默认默认的colormap
norm: [ None | Normalize ]
一个matplotlib.colors.Normalize 实例，将
data 缩放成色彩，若norm=None 且
colors=None，使用默认线性缩放（linear
scaling）
vmin, vmax: [ None | scalar(标量) ]
若不为None，其他值将被
matplotlib.colors.Normalize 实例支持，根
据levels 覆盖默认缩放色彩
levels: [level0, level1, ..., leveln]
绘制一系列指定值等值线，例如：
levels=[0]只绘制0 等值线
origin:
[ None | ‘upper’ | ‘lower’
| ‘image’ ]
若为None，Z 的第一个值将对应的左下角
(0,0)位置。若为‘image’，将使用
image.origin 对应的rc 值。若x、y 被
contour（）函数调用，该参数不会被激活
extent: [ None | (x0,x1,y0,y1) ]
若origin 为None，extent 值来自
matplotlib.pyplot.imshow()：提供像素坐标
位置，这种情况下，Z[0,0]是像素中心位置，
不是角落；若origin=None，(x0, y0)是
Z[0,0]位置,(x1, y1)是Z[-1,-1]位置。若x、
y 被contour（）函数调用，该参数不会被
激活
locator:
[ None | ticker.Locator
subclass ]
若locator=None，使用默认
MaxNLocator。如果没有指定levels，该参
数用于指定等值线levels
extend:
[ ‘neither’ | ‘both’ | ‘min’
| ‘max’ ]
Unless this is ‘neither’, contour levels are
automatically added to one or both ends of
the range so that all data are included.
These added ranges are then mapped to the
special colormap values which default to
the ends of the colormap range, but can be
set via
matplotlib.colors.Colormap.set_under() and
matplotlib.colors.Colormap.set_over()
methods.
xunits, yunits:
[ None | registered units
]
通过指定
matplotlib.units.ConversionInterface 实例，
覆盖axis 单位
antialiased: [ True | False ] 抗锯齿
仅本函数有的重要参量：
linewidths: [None|number|一系列元组或列表]
若为None，使用默认线宽；
若为number，采用统一指
定线宽；若为列表或元组，
不同线宽按列表顺序排列
linestyles:
[ None | ‘solid’ | ‘dashed’ | ‘dashdot’ |
‘dotted’ ]
27. matplotlib.pyplot.contourf(*args, **kwargs)
与contourf()函数类似：
colors:
[ None | string |
(mpl_colors) ]
若为None，颜色分配（colormap）由
cmap 指定；若为一个色彩字符串，如
colors=‘r’或colors=‘red’，则所以标签显
示这种颜色；若为一系列颜色（色彩字符
串或数组），则不同等值线按顺序绘制不
同色彩
alpha: 0-1 透明度
cmap: [ None | Colormap ]
一个colormap 实例，若cmap=None 且
colors=None，则使用默认默认的colormap
norm: [ None | Normalize ]
一个matplotlib.colors.Normalize 实例，将
data 缩放成色彩，若norm=None 且
colors=None，使用默认线性缩放（linear
scaling）
vmin, vmax: [ None | scalar(标量) ]
若不为None，其他值将被
matplotlib.colors.Normalize 实例支持，根
据levels 覆盖默认缩放色彩
levels: [level0, level1, ..., leveln]
绘制一系列指定值等值线，例如：
levels=[0]只绘制0 等值线
origin:
[ None | ‘upper’ | ‘lower’
| ‘image’ ]
若为None，Z 的第一个值将对应的左下角
(0,0)位置。若为‘image’，将使用
image.origin 对应的rc 值。若x、y 被
contour（）函数调用，该参数不会被激活
extent: [ None | (x0,x1,y0,y1) ]
若origin 为None，extent 值来自
matplotlib.pyplot.imshow()：提供像素坐标
位置，这种情况下，Z[0,0]是像素中心位置，
不是角落；若origin=None，(x0, y0)是
Z[0,0]位置,(x1, y1)是Z[-1,-1]位置。若x、
y 被contour（）函数调用，该参数不会被
激活
locator:
[ None | ticker.Locator
subclass ]
若locator=None，使用默认
MaxNLocator。如果没有指定levels，该参
数用于指定等值线levels
extend:
[ ‘neither’ | ‘both’ | ‘min’
| ‘max’ ]
Unless this is ‘neither’, contour levels are
automatically added to one or both ends of
the range so that all data are included.
These added ranges are then mapped to the
special colormap values which default to
the ends of the colormap range, but can be
set via
matplotlib.colors.Colormap.set_under() and
matplotlib.colors.Colormap.set_over()
methods.
xunits, yunits:
[ None | registered units
]
通过指定
matplotlib.units.ConversionInterface 实例，
覆盖axis 单位
antialiased: [ True | False ] 抗锯齿
仅本函数有的重要参量：
nchunk: [ 0 | integer ]
若为0，区域不进行细分。表征填充区域
划分精度
hatches:
一系列交叉填充图案。若为None，没有
hatch 添加到contour。Hatching 仅被
PostScript, PDF, SVG 和Agg backends 支持
28. matplotlib.pyplot.csd(x, y, NFFT=256, Fs=2, Fc=0, detrend=<function
detrend_none at 0x2635de8>, window=<function window_hanning at
0x2635b90>, noverlap=0, pad_to=None, sides='default', scale_by_freq=Non
e, hold=None, **kwargs)
绘制交叉谱密度。
csd(x, y, NFFT=256, Fs=2, Fc=0, detrend=mlab.detrend_none, window=
mlab.window_hanning, noverlap=0, pad_to=None, sides='default',
scale_by_freq=None, **kwargs)
交叉谱密度基于 Welch’s 平均周期图法。
NFFT: integer
每排的FFT 数据点数。必须均匀，2 的平方最有
效，默认值256。不能用于获取零填充（补零），
否则结果错误， 使用pad_to 替代。
Fs: scalar 标量
采样频率（样品每时间单位[time unit]）。它是
用来计算傅立叶频率,freqs,周期中每时间单位(in
cycles per time unit)。默认值为2
detrend: callable？ 应用在每个fft 之前的函数，旨在消除平均或线性
趋势。不像在MATLAB，detrend 参数是一个向
量，而在matplotlib 是一个函数。Pylab 模块定
义了detrend_none(), detrend_mean(), and
detrend_linear()函数，也可以用户自定义。
window:
callable 或
ndarray（pytho
n 的某种数组）
一个函数或向量的傅立叶变换长度。创建窗口向
量出现window_hanning()，window_none()，
NumPy.blackman()，NumPy.hamming()，
NumPy.bartlett()，SciPy.signal()，
scipy.signal.get_window(),等。默认的是
window_hanning()，如果一个函数作为参数传递，
它必须以一个数据段作为参数和返回段的窗口版
本。
pad_to: integer
执行FFT 时，数据段垫点的个数。不同于
NFFT 指定使用的数据点的数目。在不增加
PSD 的实际分辨率（分辨峰之间的最小距离）的
情况下，可以在图中获取更多的点。这对应于调
用fft()中的n 参数，默认为None，可设置
pad_to 使等同于NFFT。
sides:
[ ‘default’ |
‘onesided’ |
‘twosided’ ]
指定返回哪一侧的PSD，默认‘default’，它返回
one-sided 真实数据和复杂的数据。'one-sided'返
回一侧， ‘twosided’返回两侧
scale_by_freq: boolean 布尔数
指定是否返回由缩放频率缩放的密度值，单位
Hz^-1。这允许集成在返回的频率值。
MATLAB 兼容默认是True 的。
noverlap: integer
重叠的块之间的点的数量。默认值是0（无重叠）
。
Fc: integer
X 的中心频率（缺省为0）。offsets the x extents
of the plot to reflect the frequency range used
when a signal is acquired and then filtered and
downsampled to baseband.
其他参数参见 Line2D属性。
29. matplotlib.pyplot.delaxes(*args)
删除当前figure 的axes
30. matplotlib.pyplot.draw()
重新绘制当前figure，用于交互式环境。
fig.canvas.draw()
31. matplotlib.pyplot.errorbar(x, y, yerr=None, xerr=None, fmt='-
', ecolor=None, elinewidth=None, capsize=3, barsabove=False,lolims=False,
uplims=False, xlolims=False, xuplims=False, errorevery=1, capthick=None, h
old=None, **kwargs)
绘制errorbar 图表，如图：
errorbar(x, y, yerr=None, xerr=None,fmt='-', ecolor=None,
elinewidth=None, capsize=3, barsabove=False, lolims=False, uplims=False,
xlolims=False, xuplims=False, errorevery=1, capthick=None)
yerr 表示垂直方向的误差线，xerr 表示水平方向的误差线。
可选重要参量：
xerr/yerr:
[ scalar | N, Nx1, or 2xN
array-like ]
若为标量(scalar)、N、Nx1 数组，
errobar 绘制各个点对应的+/-值；若为
2xN，errorbar 绘制为-row1 和
row2(row1 为2xN 的第一列，row2 为
2xN 的第二列)
fmt: ‘-‘ errobar 绘制线型符号
ecolor: [ None | mpl color ] matplotlib 色彩
elinewidth: scalar errobar 的线宽，类似与linewidth
capsize: scalar errobar 棒长
capthick: scalar errobar 棒cap 的宽度
barsabove: [ True | False ]
errobar 图层在plot 图层的上(True)、
下(False)
lolims / uplims / xlol
ims / xuplims:
[ False | True ]
These arguments can be used to indicate
that a value gives only upper/lower
limits. In that case a caret symbol is used
to indicate this. lims-arguments may be
of the same type as xerr and yerr.
errorevery:
positive integer(正整数)
errobar 长度
其他参量参见 Line2D属性
32matplotlib.pyplot.eventplot(positions, orientation='horizontal', lineoffse
ts=1, linelengths=1, linewidths=None, colors=None,linestyles='solid', hold=N
one, **kwargs)
绘制条形线、条形码。
eventplot(positions, orientation='horizontal', lineoffsets=0,
linelengths=1, linewidths=None, color =None, linestyles='solid'
在给定的位置绘制平行线，位置由一维或二维数组指定，每行对应一个row 或column。
用于离散事件分析。
orientation :
[ ‘horizonal’ | ‘vertical’ ]水平或垂直
方向
‘horizonal’使每一个条形横放、
整体呈垂直排列；‘vertical’使每
一个条形竖放、整体呈水平排列
lineoffsets : float(浮点数)或浮点型数组序列线性偏移
linelengths : float(浮点数)或浮点型数组序列线长
linewidths : float(浮点数)或浮点型数组序列线宽
colors RGBA 元组序列、及列表数组
linestyles :
[ ‘solid’ | ‘dashed’ | ‘dashdot’ |
‘dotted’ ]或这些参数列表
其他属性为LineCollection属性，参见PolyCollection实例。
33. matplotlib.pyplot.figimage(*args, **kwargs)
为figure 添加非重采样图像(non-resampled image)
figimage(X, xo, yo)
X 必须是浮点型数组
若X is MxN, 默认亮度（灰度）
若X is MxNx3, 默认RGB
若X is MxNx4, 默认RGBA
Keyword 描述
xo or yo 整形数，x、y 方向像素偏移量
cmap Colormap 实例，例如cm.jet.默认为rc image.cmap
norm Normalize 实例，默认normalization()，该标量亮度0-1
vmin|vmax
用于缩放图像以0-1 的亮度。若为None，亮度值的最小值和最
大值将被使用。注意，如果你通过一个规范的实例，为Vmin 和
Vmax 的设置将被忽略。
alpha α-混合值，默认为None
origin [ ‘upper’ | ‘lower’ ]，指定[0,0]点的位置为左上角或左下角
34. matplotlib.pyplot.figlegend(handles, labels, loc, **kwargs)
为figure 添加标题
labels 一列字符串
handles 一系列 Line2D 或Patch 实例（参见 Line2D 实例）
loc 指定标签位置，可以是整形数，也可以是形如'upper right'的字符串
loc 参数：
Location
String
Location Code
‘best’ 0
‘upper right’ 1
‘upper left’ 2
‘lower left’ 3
‘lower right’ 4
‘right’ 5
‘center left’ 6
‘center right’ 7
‘lower center’ 8
‘upper center’ 9
‘center’ 10
35. matplotlib.pyplot.figtext(*args, **kwargs)
为figure 添加text：
text(x, y, s, fontdict=None, **kwargs)
x、y 坐标（0-1 表示位于figure 位置）表示text 所在位置。
具体参数查看 Text属性
36.
matplotlib.pyplot.figure(num=None, figsize=None, dpi=None, facecolor=N
one, edgecolor=None, frameon=True, FigureClass=<class
'matplotlib.figure.Figure'>, **kwargs)
创建一个新figure
num 整数或字符串，可选，默认None。Num=n 选择图表n 或创建图表n
figsize 创建指定大小的图像，单位：尺寸。Figuresize=(m,n)
dpi 每英寸的像素数，默认80
facecolor 背景颜色
edgecolor 边框颜色
返回Figure 参数。
37. matplotlib.pyplot.fill(*args, **kwargs)
绘制填充图，填充x 轴和曲线y 之间区域。
ax.fill(x1, y1, 'g', x2, y2, 'r')
返回Polygon属性，其他参数可见Polygon实例。
38.matplotlib.pyplot.fill_between(x, y1, y2=0, where=None, interpolate=False, ho
ld=None, **kwargs)
绘制填充图，填充x 区间内不同曲线之间区域。
fill_between(x, y1, y2=0, where=None, **kwargs)
x 指定填充区间，填充y1 和y2 之间部分。其中where 是填充条件，如where=y1>y2，则仅
绘制y1 与y2 之间区域；若为None，则对数组x 指定范围全部填充；若为布尔数组，则填
充True 部分。
interpolate 表示插入额外区域，默认为False，针对的是x 轴区间范围。
其它参数参见Polygon属性。
39.matplotlib.pyplot.fill_betweenx(y, x1, x2=0, where=None, hold=Non
e, **kwargs)
绘制填充图，填充y 区间内不同x 的函数曲线之间区域，如图：
fill_betweenx(y, x1, x2=0, where=None, **kwargs)
y 指定填充区间，填充x1 和x2 之间部分。其中where 是填充条件，如where=y1>y2，则仅
绘制y1 与y2 之间区域；若为None，则对数组x 指定范围全部填充；若为布尔数组，则填
充True 部分。
interpolate 表示插入额外区域，默认为False，针对的是x 轴区间范围。
其它参数参见Polygon属性。
40. matplotlib.pyplot.findobj(o=None, match=None, include_self=True)
寻找artist 目标。递归寻找所有包含在self 的Artist 实例。
match
None:返回包含artist 的所有目标；利用布尔函数boolean =
match(artist)过滤匹配；class 实例，例如Line2D，只返回class 类型
若include_self= True，将确认匹配项的self 属性
41. matplotlib.pyplot.gca(**kwargs)
返回当前axes 实例，可用于控制axis 属性、或通过set 函数设置
plot(t,s)
set(gca(), 'xlim', [0,10])
或
plot(t,s)
a = gca()
a.set_xlim([0,10])
42. matplotlib.pyplot.gcf()
返回当前figure 实例。
43. matplotlib.pyplot.gci()
获取当前色彩artist，返回当前 ScalarMappable实例(image 或patch collection)
44. matplotlib.pyplot.get_figlabels()
返回 figure 标签列表
45. matplotlib.pyplot.get_fignums()
返回所有figure 数字排列序列
46. matplotlib.pyplot.grid(b=None, which='major', axis='both', **kwargs)
是否显示图轴网格
grid(self, b=None, which='major', axis='both', **kwargs)
主要参数：
b [True|False]或者是布尔数组、或[‘on’,‘off’] 表示网格是否开启
which [major(默认)|minor|both] 选择主、次网格开启方式
axis [both(默认)|x|y] 选择使用网格的数轴
其他参数见Line2D 属性。
47.matplotlib.pyplot.hexbin(x, y, C=None, gridsize=100, bins=None, xscale='linear',
yscale='linear', extent=None, cmap=None,norm=None, vmin=None, vmax=None, alpha=
None, linewidths=None, edgecolors='none', reduce_C_function=<function mean at 0x16
ea d70>, mincnt=None, marginals=False, hold=None, **kwargs)
绘制hexagonal binning 图：
hexbin(x, y, C = None, gridsize = 100, bins = None,xscale = 'linear',
yscale = 'linear',cmap=None, norm=None, vmin=None,
vmax=None,alpha=None, linewidths=None,
edgecolors='none',reduce_C_function = np.mean, mincnt=None,
marginals=True,**kwargs)
必须参数：
x、y 一维等长数组
C
[None(默认)|与x、y 等
长的一维数组]
若C=None，在(x[i],y[i])生成对应柱状图；若C 为指定
值，则根据相关函数指定(x[i],y[i])点值，这些值根据
reduce_C_function 运算
可选参数：
gridsize:
[ 100(默认) | integer
]
x 方向的hexagons 数，默认100，而y 方向自动调
整与x 方向相近似。另外，也可以指定一个两个元
素的元组(a,b),分别指定x、y 方向
bins:
[ None | ‘log’ |
integer | sequence ]
若为None，不进行分级，每个hexagon 的色彩值直
接对应计算值；若为‘log’，使用对数刻度的color
map，用来确定hexagon 内部颜色；若为interger，
按指定值划分bins 值，并自动分配hexagons 颜色；
若为一系列值，将使用bins 下边界值
xscale: [ ‘linear’ | ‘log’ ] 指定水平轴线性刻度、指数刻度
scale: [ ‘linear’ | ‘log’ ] 指定垂直轴线性刻度、指数刻度
mincnt:
[ None | 一个正整数
]
若不为None,只显示超过mincnt 数的cell
marginals: [ True | False ]
若为True，根据colormap 矩形框、沿着x 轴(底部)
和y 轴(左侧)绘制边缘密度
extent:
[ None | scalars (left,
right, bottom, top) ]
bins 的限制（取值范围），默认根据gridsize, x, y,
xscale,yscale 分配
其他参量控制color mapping 和normalization 参量：
cmap Colormap 实例，例如cm.jet.默认为rc image.cmap
norm Normalize 实例，默认normalization()，该标量亮度0-1
vmin|vmax
用于缩放图像以0-1 的亮度。若为None，亮度值的最小值和最大值将被使用。注
意，如果你通过一个规范的实例，为Vmin 和Vmax 的设置将被忽略。
alpha α-混合值，默认为None
linewidths: [ None | scalar ]
其他参量控制Collection 属性：
edgecolors:
[ None | 'none' | mpl color | color
sequence ]
若为'none'，边缘绘制相同颜色；若
为None，使用默认颜色填充
outlines；若为matplotlib 颜色或序列
RGBA 元组，用指定颜色绘制outlines
其他参量参见PolyCollection实例。
48.matplotlib.pyplot.hist(x, bins=10, range=None, normed=False, weights=
None, cumulative=False, bottom=None,histtype='bar', align='mid', orientatio
n='vertical', rwidth=None, log=False, color=None, label=None, stacked=False,
hold=None,**kwargs)
绘制直方图
x : array_like, shape (n, ) 形如shape(n,)的数组
bins :
integer 或array_like, 可选,
默认: 10
若给定一个整数，返回一个bins+1 个条柱
range :
tuple(元组), 可选, 默认:
None
bins(横坐标)的最大、最小值，默认
range=(x.min(), x.max())
normed : 布尔量，可选，默认：False 若为True，按正常方式处理直方图
weights :
形如shape(n,)的数组，可选,
默认: None
权重数组，与x 数组长度一致
cumulative : 布尔量，可选，默认
histtype :
[‘bar’ | ‘barstacked’ | ‘step’ |
‘stepfilled’], optional
‘bar’是一个杆式直方图；“barstacked '是一
个条形直方图有多个数据堆叠在彼此顶部；
‘step’生成悬空未填充线条；“stepfilled”产
生填充线条
align :
[‘left’ | ‘mid’ | ‘right’],可选，
默认: ‘mid’
指定条(bar)的起点位置，左侧、中间、右
侧
orientation : [‘horizontal’ | ‘vertical’], optional
rwidth :
scalar, optional, default:
None
相对宽度
log : boolean, optional, default x 轴是否为log 刻度
color : color or array_like of colors, 可选，默认: None
label : 字符串，可选，默认：‘’
stacked : 布尔参量，可选，默认
其他参量参见 matplotlib.patches.Polygon 实例。
49.matplotlib.pyplot.hist2d(x, y, bins=10, range=None, normed=False, weig
hts=None, cmin=None, cmax=None, hold=None,**kwargs)
绘制二维直方图
x, y: array_like, shape (n, ) : 形如shape(n,)的数组
bins:
[None | int | [int, int] |
array_like | [array, array]] :
若为int，指定两维的bins(nx=ny=bins)；若
为[int,int]分别指定x、y 方向bins(nx, ny =
bins)；若为数组(array_like)，指定两维的
bin:(x_edges=y_edges=bins)；若为
[array,array],指定x、y 方向bins：(x_edges,
y_edges = bins)
range :
形如shape(2,2)的数组, 可选,
默认: None
指定区间[[xmin, xmax], [ymin, ymax]]，这个
范围以外的所有的值都被认为是不吻合直方
图的异常值
normed : 布尔型，可选，默认：False 归一化直方图
weights :
形如shape(n,)的数组，可选，
默认：None
每个(x_i, y_i)点对应的权重值
cmin : 标量，可选，默认：None
所有低于cmin 值的bins 将不被显示，并且
返回值被设为nan upon
cmax : 标量，可选，默认：None
所有高于cmax 值的bins 将不被显示，并且
返回值被设为nan upon
返回值为：'(counts, xedges, yedges, Image)'
50.matplotlib.pyplot.hlines(y, xmin, xmax, colors='k', linestyles='solid', lab
el='', hold=None, **kwargs)
从每个y 区间的xmin 到xmax 范围绘制水平线
参数：
y : 标量或一维数组指定绘图区间
xmin,
xmax :
标量或一维数组
指定每条线的起始值、终止值，若给定
了标量：所有线长度相同
colors : 形如数组颜色，可选，默认：'k'
linestyles :
[‘solid’ | ‘dashed’ | ‘dashdot’ |
‘dotted’], 可选
label : 字符串，可选，默认: ‘’
其他参数见 LineCollection实例。
51. matplotlib.pyplot.hold(b=None)
b=None 默认为True。若为True，之后的命令也会被添加到当前axes；若为False，当前
axes 命令会被清除，而只绘制接下来输入的命令。
52. matplotlib.pyplot.imread(*args, **kwargs)
读取image 图像文件，将信息转化为数组。
fname 文件路径必须以二进制形式打开
读取形式None,r+,r,w+,c
53. matplotlib.pyplot.imsave(*args, **kwargs)
参数：
fname: 文件路径及文件名
arr: 一个M×N（亮度），mxnx3（RGB）或mxnx4（RGBA）数组。
重要参数：
vmin/vmax: [ None | scalar ] 设置颜色范围区间
cmap:
Colormap 实例，若为None，默认rc
image.cmap
format:
一个由后台支持的文件扩展名。大多数的后台
支持PNG，PDF，PS，EPS 和SVG。
origin [ ‘upper’ | ‘lower’ ] 指定[0,0]点的位置为左上、左下
dpi
存储在文件元数据的DPI。这并不影响输出图像
的分辨率。
54matplotlib.pyplot.imshow(X, cmap=None, norm=None, aspect=None, inte
rpolation=None, alpha=None, vmin=None,vmax=None, origin=None, extent=
None, shape=None, filternorm=1, filterrad=4.0, imlim=None, resample=None,
url=None,hold=None, **kwargs)
在axes 上显示image 图像
cmap : Colormap,可选，默认：None
若为None，默认rc image.cmap 值。
当x 包含RGB(A)信息，cmap 则被忽
略
aspect :
[‘auto’ | ‘equal’ | scalar],可选，
默认：None
若为‘auto’，改变图像的纵横比以匹
配当前axes；若为‘equal’，(1)且
extent=None，改变图像的纵横比以
匹配当前axes,(2)若extent 不为
None，改变axes 的纵横比以匹配当
前图像；若为None,默认rc
image.aspect 值
interpolation :
None(默认),字符串,可选:
‘none’, ‘nearest’, ‘bilinear’,
‘bicubic’, ‘spline16’,
‘spline36’, ‘hanning’,
‘hamming’, ‘hermite’, ‘kaiser’,
‘quadric’, ‘catrom’, ‘gaussian’,
‘bessel’, ‘mitchell’, ‘sinc’,
‘lanczos’
若interpolation=None(无差值),默认
为rc image.interpolation。详情参见
手册
norm : Normalize,可选,默认: None
通过Normalize 实例，使亮度标准化
为0-1;若为None,使用默认
func:normalize 标准化方法。
Norm 仅用于x 是浮点数组
vmin, vmax : 标量，可选，默认：None
指定亮度范围，若norm 实例已指定，
该参数无效
alpha : 标量，可选，默认：None 0-1
origin :
[‘upper’ | ‘lower’],可选，默认：
None
指定[0,0]点的位置为左上、左下
extent :
标量(left, right, bottom, top)，
可选，默认：None
指定x、y 像素中心
shape :
标量(columns, rows)，可选，
默认：None
详情见参考书
filternorm : 标量，可选，默认：1 详情见参考书
filterrad : 标量，可选，默认：4 详情见参考书
返回值：image : AxesImage
55. matplotlib.pyplot.ioff()
关闭绘图交互模式
56 matplotlib.pyplot.ion()
打开绘图交互模式
57. matplotlib.pyplot.ishold()
返回当前axes 的hold 状态
58. matplotlib.pyplot.isinteractive()
返回绘图交互模式状态
59. matplotlib.pyplot.legend(*args, **kwargs)
在当前axes 放置 legend 标签
legend((line1, line2, line3), ('label1', 'label2', 'label3'), loc=2,
bbox_to_anchor = (0.5, 0.5))
其中(line1, line2, line3)和('label1', 'label2', 'label3')一一对应。loc 表示标签位置：
Location String Location Code
‘best’ 0
‘upper right’ 1
‘upper left’ 2
‘lower left’ 3
‘lower right’ 4
‘right’ 5
‘center left’ 6
‘center right’ 7
‘lower center’ 8
‘upper center’ 9
‘center’ 10
而bbox_to_anchor=(m,n)指定具体位置:m 表示横坐标位置(0-1 之间)，n 表示
纵坐标位置(0-1 之间)。
numpoints: 整数 legend 图例中line 的点数
scatterpoints: 整数 scatter 图legend 图例中点数
scatteryoffsets: 浮点数列表yoffsets 列表
markerscale: [ None | scalar ] 相对于原图例标记的大小
frameon: [ True | False ] 图例边框是否开启
fancybox: [ None | False | True ] 图例边框是否花边
shadow: [ None | False | True ] 图例阴影是否开启
framealpha: [None | float] 图例透明度
ncol : 整数列数，默认：1
mode : [ “expand” | None ]
若为'expand'，图例将沿横轴
扩展到最长
bbox_to_anchor:
BboxBase 实例或2or4 个字符型
元组
？
bbox_transform :
[ an instance of Transform
| None ]
？
title 注释标题
图例内部各个元素的参数设置：
Keyword
borderpad 边框和说明文字间距
labelspacing 图例和行距
handlelength 图例中线条(legend handles)的长度
handletextpad 线条(legend handles)和文字的间距(文字和符号间距)
borderaxespad 图例和最靠近的两条坐标轴间距
columnspacing 不同列的间距
60. matplotlib.pyplot.locator_params(axis='both', tight=None, **kwargs)
控制轴刻度标签
关键参数：
axis [‘x’ | ‘y’ | ‘both’] 选择坐标轴，默认：‘both’
tight [True | False | None]
默认为None，不改变。将参量传输到
autoscale_view()
其他参量直接访问set_params()。
61. matplotlib.pyplot.loglog(*args, **kwargs)
x、y 轴均为指数刻度
重要参数：
basex/basey: scalar > 1 x、y 轴刻度的底数
subsx/subsy: [ None | sequence ]
x、y 轴刻度最小值。详情参见
matplotlib.axes.Axes.set_xscale() /
matplotlib.axes.Axes.set_yscale()
nonposx/nonposy
:
[‘mask’ (屏蔽)|
‘clip’(转换)]
屏蔽小于0 的数；或转换负数转换为接近0 的
数
其他参量参见Line2D属性。
62. matplotlib.pyplot.margins(*args, **kw)
设置或检索自动缩放功能。返回值xmargin, ymargin
margins(margin)
margins(xmargin, ymargin)
margins(x=xmargin, y=ymargin)
margins(..., tight=False)
63. matplotlib.pyplot.matshow(A, fignum=None, **kw)
将数组以矩阵的形式在figure 窗口显示。
fignum: [ None | integer | False ]
布尔型数组，默认创建一个新的窗口；若为
False 或0，则不创建
origin [‘upper’ | ‘lower’],可选,默认,None 指定[0,0]的位置
64. matplotlib.pyplot.minorticks_off()
关闭轴线次刻度
65. matplotlib.pyplot.minorticks_on()
开启轴线次刻度
66. matplotlib.pyplot.over(func, *args, **kwargs)
以hold(True)的方式调用函数，并保存当前状态。
67. matplotlib.pyplot.pause(interval)
暂停指定时间，单位：秒
68. matplotlib.pyplot.pcolor(*args, **kwargs)
绘制二维数组的伪彩色图。
pcolor(C, **kwargs)
pcolor(X, Y, C, **kwargs)
C 为颜色值。重要参数：
cmap :
Colormap,可选，
默认：None
若为None，默认rc image.cmap 值。当x 包含
RGB(A)信息，cmap 则被忽略
norm :
Normalize,可选,默
认: None
通过Normalize 实例，使亮度标准化为0-1;若为
None,使用默认func:normalize 标准化方法。
Norm 仅用于x 是浮点数组
vmin, vmax :
标量，可选，默认：
None
指定亮度范围，若norm 实例已指定，该参数无
效
shading: [ ‘flat’ | ‘faceted’ ]
若为 ‘faceted’，在每个矩形绘制一个黑色的网格；
若为‘flat’，边缘将不绘制。
edgecolors:
[ None | 'none' |
color | color
sequence]
若为None,使用默认rc 设置；若为'none'，边缘
不可见；一个MPL 序列色彩设置边缘的颜色
alpha:
0 <= scalar <= 1
或None
透明度混合值
其他参数见 PolyCollection实例。
69. matplotlib.pyplot.pcolormesh(*args, **kwargs)
绘制一个四边形网格，创建一个二维阵列的伪彩色图。
pcolormesh(C)
pcolormesh(X, Y, C)
pcolormesh(C, **kwargs)
cmap :
Colormap,可选，默
认：None
若为None，默认rc image.cmap 值。当x 包含
RGB(A)信息，cmap 则被忽略
norm :
Normalize,可选,默认:
None
通过Normalize 实例，使亮度标准化为0-1;若为
None,使用默认func:normalize 标准化方法。
Norm 仅用于x 是浮点数组
vmin, vmax :
标量，可选，默认：
None
指定亮度范围，若norm 实例已指定，该参数无
效
shading: [ ‘flat’ | ‘faceted’ ]
若为 ‘faceted’，在每个矩形绘制一个黑色的网格；
若为‘flat’，边缘将不绘制。
edgecolors:
[ None | 'none' |
color | color
sequence]
若为None,使用默认rc 设置；若为'none'，边缘
不可见；一个MPL 序列色彩设置边缘的颜色
alpha:
0 <= scalar <= 1 或
None
透明度混合值
其他参数见PolyCollection实例：
70.matplotlib.pyplot.pie(x, explode=None, labels=None, colors=None, auto
pct=None, pctdistance=0.6, shadow=False,labeldistance=1.1, startangle=No
ne, radius=None, hold=None)
绘制饼图
pie(x, explode=None, labels=None, colors=('b', 'g', 'r', 'c', 'm', 'y',
'k', 'w'), autopct=None, pctdistance=0.6, shadow=False,
labeldistance=1.1, startangle=None, radius=None)
关键参数：
explode: [ None | len(x) sequence ] 指定饼图分片数
colors: [ None | color sequence ] matplotlib 颜色
labels:
[ None | len(x) sequence of
strings ]
指定每个分片的标签
autopct:
[ None | format string | format
function ]
标签中数字格式
pctdistance: 标量饼图和标签比例？默认0.6
labeldistance: 标量标签距饼中心距离
shadow: [ False | True ] 饼图阴影
startangle: [ None | Offset angle ]
若不为None，从x 轴起逆时针旋
转
radius: [ None | scalar ] 饼的半径，默认为：1
71. matplotlib.pyplot.plot(*args, **kwargs)
详情参见 Line2D属性
72.matplotlib.pyplot.plot_date(x, y, fmt='bo', tz=None, xdate=True, ydate
=False, hold=None, **kwargs)
绘图添加日期。该函数类似于plot()函数，除了x 或y(或者两者)为日期，并且轴坐
标标签也是日期。
关键参数：
fmt: string 图格式字符串
tz:
[ None | timezone(时区) string
| tzinfo instance]
时区使用标记的日期；若为None,默
认rc 值
xdate: [ True | False ] 若为True，x 轴标记日期
ydate: [ True | False ] 若为True，y 轴标记日期
其他参数见Line2D属性。
73.matplotlib.pyplot.plotfile(fname, cols=(0, ), plotfuncs=None, commen
ts='#', skiprows=0, checkrows=5, delimiter=', ',names=None, subplots=True,
newfig=True, **kwargs)
直接绘制文件数据(跳过读取文件命令)。
cols：用于鉴别column 内容。若为int，表征列数；若为字符串，表征列标题(label)，列标
题的空格符将被'_'下划线取代，并删除所以非法字符。
若len(cols) == 1，仅绘制指定列(column)的数据；若len(cols) > 1，第一列
(column)数据作为x 轴数据，其他列数据分别为y 轴数据：若subplots=True，分别将
各个曲线绘制在多个子图；若subplots=False ，多个曲线只绘制在一个图。
plotfuncs
‘plot’,‘semilogy’, ‘fill’,
‘bar’,等
选择绘图函数
newfig [ True | False ]
若为True，每次plot 都创建新绘图窗口；若为
False，在原窗口绘图
comments, skiprows, checkrows, delimiter, 和names 通过
matplotlib.pylab.csv2rec()将数据载入到记录数组。
74. matplotlib.pyplot.polar(*args, **kwargs)
绘制极坐标图
polar(theta, r, **kwargs)
theta 表示旋转角度，r 表示旋转半径
75.matplotlib.pyplot.psd(x, NFFT=256, Fs=2, Fc=0, detrend=<function
detrend_none at 0x2635de8>, window=<function window_hanning at
0x2635b90>, noverlap=0, pad_to=None, sides='default', scale_by_freq=Non
e, hold=None, **kwargs)
绘制功率谱密度图
psd(x, NFFT=256, Fs=2, Fc=0, detrend=mlab.detrend_none,
window=mlab.window_hanning, noverlap=0, pad_to=None,sides='default',
scale_by_freq=None, **kwargs)
向量x 分割进入NFFT 长度分段，每个分段通过detrend 函数和window 函数去趋势，
noverlap 为每段重叠区域长度。通过每段的平均值来计算Pxx，通过一个标准
来纠正功率损耗。Fs 是采样频率。关键参数：
NFFT: integer
每排的FFT 数据点数。必须均匀，2 的平方最有
效，默认值256。不能用于获取零填充（补零），
否则结果错误， 使用pad_to 替代。
Fs: scalar 标量
采样频率（样品每时间单位[time unit]）。它是
用来计算傅立叶频率,freqs,周期中每时间单位(in
cycles per time unit)。默认值为2
detrend: callable？
应用在每个fft 之前的函数，旨在消除平均或线性
趋势。不像在MATLAB，detrend 参数是一个向
量，而在matplotlib 是一个函数。Pylab 模块定
义了detrend_none(), detrend_mean(), and
detrend_linear()函数，也可以用户自定义。
window:
callable 或
ndarray（pytho
n 的某种数组）
一个函数或向量的傅立叶变换长度。创建窗口向
量出现window_hanning()，window_none()，
NumPy.blackman()，NumPy.hamming()，
NumPy.bartlett()，SciPy.signal()，
scipy.signal.get_window(),等。默认的是
window_hanning()，如果一个函数作为参数传递，
它必须以一个数据段作为参数和返回段的窗口版
本。
pad_to: integer
执行FFT 时，数据段垫点的个数。不同于
NFFT 指定使用的数据点的数目。在不增加
PSD 的实际分辨率（分辨峰之间的最小距离）的
情况下，可以在图中获取更多的点。这对应于调
用fft()中的n 参数，默认为None，可设置
pad_to 使等同于NFFT。
sides:
[ ‘default’ |
‘onesided’ |
‘twosided’ ]
指定返回哪一侧的PSD，默认‘default’，它返回
one-sided 真实数据和复杂的数据。'one-sided'返
回一侧， ‘twosided’返回两侧
scale_by_freq: boolean 布尔数
指定是否返回由缩放频率缩放的密度值，单位
Hz^-1。这允许集成在返回的频率值。
MATLAB 兼容默认是True 的。
noverlap: integer
重叠的块之间的点的数量。默认值是0（无重叠）
。
Fc: integer
X 的中心频率（缺省为0）。offsets the x extents
of the plot to reflect the frequency range used
when a signal is acquired and then filtered and
downsampled to baseband.
其他参数参见Line2D属性。
76. matplotlib.pyplot.quiver(*args, **kw)
绘制二维箭头
quiver(X, Y, U, V, C, **kw)
X, Y:
箭头的位置坐标(默认是箭头尾部)
U, V: 箭头在x、y 方向的分量
C: 箭头颜色，可选参量
重要参量：
units:
[ ‘width’ | ‘height’ |
‘dots’ | ‘inches’ | ‘x’ | ‘y’ |
‘xy’ ]
箭头的尺寸单位；除了长度都是这个单位的倍数。
‘width’或‘height’:axes 的宽度或长度；‘dots’或
‘inches’:基于figure 的像素或英寸；‘x’,‘y’,或‘xy’:X,
Y,或sqrt(X^2+Y^2)的数据单位
若units= ‘width’或 ‘height’，调整坐标轴的长或宽，箭头大小也随之变化；若
units=‘dots’或 ‘inches’，调整坐标轴长或宽不影响箭头大小。
angles: [‘uv’|‘xy’ |array ]
默认‘uv’，箭头横纵方向比例为1：1；若
为‘xy’，箭头由(x,y)指向(x+u, y+v)；可用
数组指定箭头方向。
scale: [None|float]
浮点型数据，指定一个长度为单位长度，
如：scale=2inches(单位由scale_units 确定)
，则风速为2m/s 在图中显示为1 英寸
(inches)
scale_units:
None,或其它单位,如：
‘inches’,‘width’,‘x’
若为‘x’，则和x 轴单位一样；若为
‘width’,单位为axes 宽度
width: scalar，默认0.005
箭头宽度，与scale_units 和scale 的选取有
关，默认为0.005*图轴宽度
headwidth: scalar，默认3 尖端宽度，默认3
headlength: scalar，默认5 尖端长度，默认5
headaxislength
:
scalar，默认4.5 箭头两翼长度，默认4.5
minshaft: scalar，默认1
长度低于scale 值的箭头，尖端长度设置为
默认值1。尽量不要低于1，否则小箭头很
难看。
minlength: scalar，默认1
长度低于该值的箭头不被绘制，而只绘制
一个(六边形)点
pivot: [ ‘tail’ | ‘middle’ | ‘tip’ ] 支点位置，箭头围绕该点旋转
其他参量参见PolyCollection属性。
77. matplotlib.pyplot.quiverkey(*args, **kw)
为quiver 图像添加注释标签
quiverkey(Q, X, Y, U, label, **kw)
参量：
Q: quiver 函数的返回值
X, Y: 注释标签的位置
U: 标准风速值对应的箭头。如U=10，则出现表示10m/s 的箭头
label: 字符串标签
其他参量：
coordinates
[‘axes’|‘figure’| ‘data’
|‘inches’]
坐标系统，‘axes’和‘figure’表示(0,0)点
为坐标左下角，(1,1)点为坐标左上角；
‘data’根据quiver 图坐标确定；
‘inches’：通过该参量表征点在图的位置，
(0,0)为左下角
color: matplotlib 颜色图例箭头颜色
labelpos [‘N’ |‘S’|‘E’|‘W’] 标签相对于图例箭头'->'的位置
labelsep: 默认：0.1
箭头和标签之间的距离，单位：英寸，
默认值：0.1
labelcolor: matplotlib 颜色字体颜色
fontproperties:
字体字典：family, style,
variant, size, weight
FontProperties 参量
78. matplotlib.pyplot.rc(*args, **kwargs)
设置当前rc 参量，包括lines.linewidth 群的lines，axes.facecolor 群的
axes 等。如：
rc('lines', linewidth=2, color='r')
rcParams['lines.linewidth'] = 2
rcParams['lines.color'] = 'r'
font = {'family' : 'monospace',
'weight' : 'bold',
'size' : 'larger'}
rc('font', **font) # pass in the font dict as kwargs
79. matplotlib.pyplot.rc_context(rc=None, fname=None)
返回一个用于管理RC 设置的文管理器。
参见有关资料
80. matplotlib.pyplot.rcdefaults()
恢复默认的RC 参数。
81. matplotlib.pyplot.rgrids(*args, **kwargs)
获取或设置极坐标图的径向辐射网格线。
lines, labels = rgrids()
lines, labels = rgrids(radii, labels=None, angle=22.5, **kwargs)
当不调用参数，rgrid()仅返回元组(lines, labels)，其中lines 是辐射网格数组
(Line2D实例)，labels 是一个标签数组(Text实例)。当调用参数，标签会在指定的位置出
现(radial distances and angles)。labels 包含len(radii)组标签字符串。例子：
# set the locations of the radial gridlines and labels
lines, labels = rgrids( (0.25, 0.5, 1.0) )
# set the locations and labels of the radial gridlines and labels
lines, labels = rgrids( (0.25, 0.5, 1.0), ('Tom', 'Dick', 'Harry'
)
82. matplotlib.pyplot.savefig(*args, **kwargs)
保存当前figure
savefig(fname, dpi=None, facecolor='w', edgecolor='w',
orientation='portrait', papertype=None, format=None,
transparent=False, bbox_inches=None, pad_inches=0.1,
frameon=None)
fname：文件路径及文件名
关键参数：
dpi: [ None | scalar > 0 ] 每英寸的分辨率
facecolor, edgecolor: matplotlib 色彩figure 图像颜色
orientation: [ ‘landscape’ | ‘portrait’ ]
不支持所有的后台；目前
只能在PostScript 输出
papertype:
‘letter’, ‘legal’, ‘executive’, ‘ledger’,
‘a0’ through ‘a10’, ‘b0’ through
‘b10’等
仅支持PostScript 输出
format: 文件扩展名png, pdf, ps, eps 和svg 等.
transparent: 透明度？ ？
frameon: [True|False] 背景图是否显示
bbox_inches: ‘tight’ 边缘空白区域大小设置
pad_inches: ？ ？
bbox_extra_artists: ？ ？
83. matplotlib.pyplot.sca(ax)
设置当前axes 实例
84.matplotlib.pyplot.scatter(x, y, s=20, c='b', marker='o', cmap=None, n
orm=None, vmin=None, vmax=None, alpha=None,linewidths=None, verts=N
one, hold=None, **kwargs)
绘制散点图，其中X 和Y 是相同长度的数组序列
x, y : 形如shape (n, )数组输入数据
s :
标量或形如shape (n, )数组，可
选，默认：20
size in points^2
c : 色彩或颜色序列，可选，默认
注意C 不应是一个单一的RGB 数字或
RGBA 序列，因为不便区分。C 可以是
一个RGB 或RGBA 二维行数组
marker : MarkerStyle, 可选，默认: ‘o’ 详情参阅markers 属性
cmap : Colormap 可选，默认：None Colormap 实例
norm : Normalize 可选，默认：None 数据亮度0-1，float 数据
vmin, vmax : 标量，可选，默认：None
亮度设置，若norm 实例已使用，该
参数无效
alpha : 标量，可选，默认：None 0-1
linewidths : 标量或数组，默认：None ……
85. matplotlib.pyplot.sci(im)
设置当前图像
86. matplotlib.pyplot.semilogx(*args, **kwargs)
使图像x 轴为对数刻度。
basex: scalar > 1 x 轴对数底数
subsx: [ None | sequence ] 刻度序列
nonposx: [‘mask’ (屏蔽)| ‘clip’(转换)]
屏蔽小于0 的数；或转换负数转换为接
近0 的数
其他参数见Line2D属性。
87. matplotlib.pyplot.semilogy(*args, **kwargs)
使图像y 轴为对数刻度。
basey: scalar > 1 y 轴对数底数
subsy: [ None | sequence ] 刻度序列
nonposy: [‘mask’ (屏蔽)| ‘clip’(转换)]
屏蔽小于0 的数；或转换负数转换为接
近0 的数
其他参数见Line2D属性。
88. matplotlib.pyplot.set_cmap(cmap)
设置应用于当前image 的colormap。cmap 参数必须是一个Colormap
89. matplotlib.pyplot.setp(*args, **kwargs)
设置property 属性
90. matplotlib.pyplot.show(*args, **kw)
显示figure
91.matplotlib.pyplot.specgram(x, NFFT=256, Fs=2, Fc=0, detrend=<functi
on detrend_none at 0x2635de8>, window=<function window_hanning at
0x2635b90>, noverlap=128, cmap=None, xextent=None, pad_to=None, side
s='default',scale_by_freq=None, hold=None, **kwargs)
绘制光谱图，计算和绘制图谱在X 的数据位长度的数据分成段，每段的PSD 计算。窗函数
的窗口被施加到每个段，每一段的重叠量由noverlap 指定。
NFFT: integer
每排的FFT 数据点数。必须均匀，2 的平方最
有效，默认值256。不能用于获取零填充（补
零），否则结果错误， 使用pad_to 替代。
Fs: scalar 标量
采样频率（样品每时间单位[time unit]）。它
是用来计算傅立叶频率,freqs,周期中每时间单
位(in cycles per time unit)。默认值为2
detrend: callable？
应用在每个fft 之前的函数，旨在消除平均或
线性趋势。不像在MATLAB，detrend 参数是
一个向量，而在matplotlib 是一个函数。
Pylab 模块定义了detrend_none(),
detrend_mean(), and detrend_linear()函数，
也可以用户自定义。
window:
callable 或
ndarray（python 的
某种数组）
一个函数或向量的傅立叶变换长度。创建窗口
向量出现window_hanning()，
window_none()，NumPy.blackman()，
NumPy.hamming()，NumPy.bartlett()，
SciPy.signal()，scipy.signal.get_window(),等。
默认的是window_hanning()，如果一个函数
作为参数传递，它必须以一个数据段作为参数
和返回段的窗口版本。
pad_to: integer
执行FFT 时，数据段垫点的个数。不同于
NFFT 指定使用的数据点的数目。在不增加
PSD 的实际分辨率（分辨峰之间的最小距离）
的情况下，可以在图中获取更多的点。这对应
于调用fft()中的n 参数，默认为None，可设
置pad_to 使等同于NFFT。
sides: [ ‘default’ | 指定返回哪一侧的PSD，默认‘default’，它返
‘onesided’ |
‘twosided’ ]
回one-sided 真实数据和复杂的数据。'onesided'
返回一侧， ‘twosided’返回两侧
scale_by_freq: boolean 布尔数
指定是否返回由缩放频率缩放的密度值，单
位Hz^-1。这允许集成在返回的频率值。
MATLAB 兼容默认是True 的。
noverlap: integer
重叠的块之间的点的数量。默认值是0（无重
叠）。
Fc: integer
X 的中心频率（缺省为0）。offsets the x
extents of the plot to reflect the frequency
range used when a signal is acquired and then
filtered and downsampled to baseband.
cmap: 指定colormap 实例
一个matplotlib.colors.Colormap 实例；若为
None，使用默认rc 实例
xextent: (xmin,xmax)
xextent = (xmin,xmax)，指定image 在x 轴显
示范围。默认(0,max(bins))，其中bins 是
specgram()的返回值
返回值为(Pxx, freqs, bins, im):
bins 光谱计算对应时间点
freqs 频率数组
Pxx an array of shape (len(times), len(freqs)) of power
im AxesImage 实例
92.matplotlib.pyplot.spy(Z, precision=0, marker=None, markersize=None,
aspect='equal', hold=None, **kwargs)
spy(Z, precision=0, marker=None, markersize=None,
aspect='equal', **kwargs)
spy(Z)绘制二维数组Z 的稀疏模式(sparsity pattern)。
precision float、‘present’
只绘制|z|>precision 的数据；若为‘present’，所有数据都
被绘制
aspect
‘equal’(默认)、
‘auto’、标量
若为‘equal’，数组元素在空间均匀分配；若为‘auto’，图
像可能会充满绘图框；若为标量，直接指定每个元素占用
空间比例
其它参量参见Line2D和Line2D属性。
93. matplotlib.pyplot.stackplot(x, *args, **kwargs)
绘制区域堆叠(stacked area )图？
x 一维数组，元素个数为N
y MxN 型二维数组，或1xN 型一维数组
数据默认非堆叠，调用形式：
stackplot(x, y) # where y is MxN
stackplot(x, y1, y2, y3, y4) # where y1, y2, y3, y4, are all 1xNm
参量：
baseline :
[‘zero’, ‘sym’, ‘wiggle’,
‘weighted_wiggle’]
[‘zero’为简单的堆积图；‘sym’关于
0 对称，或称为ThemeRiver(河流图)
；‘wiggle’斜度最小平方和；
‘weighted_wiggle’每层根据大小考虑
重量。
colors : 一列色彩元组这些值用于循环填充堆积区域
其它参量与fill_between()相同。返回值r :一系列PolyCollection属性。
94. matplotlib.pyplot.stem(*args, **kwargs)
绘制stem 图。
stem(y, linefmt='b-', markerfmt='bo', basefmt='r-')
stem(x, y, linefmt='b-', markerfmt='bo', basefmt='r-')
绘制x-y 函数曲线的每个点到水平基线(y=0 轴线)的垂线，linefmt 表示垂线线型，
markerfmt 表示x-y 函数曲线的每个点的点标marker，basefmt 表示基线的线型。返回
值为(markerline, stemlines, baseline)元组。
95. matplotlib.pyplot.step(x, y, *args, **kwargs)
绘制阶图？
step(x, y, *args, **kwargs)
x、y 为一维数组，并且假设x 均匀增长。
where: [ ‘pre’ | ‘post’ | ‘mid’ ]
若为 ‘pre’，从x[i]到x[i+1]为
y[i+1]插值？若为 ‘post’，插值为
y[i]？若为‘mid’，取平均值？
其他参数与plot()相同。
96.matplotlib.pyplot.streamplot(x, y, u, v, density=1, linewidth=None, color
=None, cmap=None, norm=None, arrowsize=1, arrowstyle='-
|>', minlength=0.1,transform=None, hold=None)
绘制流线图
x, y : 一维数组生成一个均匀分布的网格
u, v : 二维数组列数与x 长度匹配；行数与y 长度匹配
density : 字符串或
控制流线之间的紧密程度(流线密度)，对于不同方
向的密度，使用[density_x, density_y]
linewidth : 数字或二维数组改变线宽
color :
matplotlib 色彩代
码，或二维数组
color 值转化为色彩使用cmap 参数
cmap : Colormap 用于将数组转化为颜色
norm : 0-1 亮度数据，用于color 值为数组时
arrowsize : float 表征箭头大小
arrowstyle : 箭头字符串箭头样式，参见FancyArrowPatch
minlength : float 流线最短长度
97. matplotlib.pyplot.subplot(*args, **kwargs)
在一个图表中绘制多个子图。
subplot(nrows, ncols, plot_number, axisbg, polar, projection)
绘图区分为nrows 行和ncols 列，从左到右、从上到下顺序对每个区域进行编号，左上区
编号为1。nrows 表示图的行数，ncols 表示图的列数，plot_number 表示第几个子图。上述
三个参数均小于10 时，中间逗号可以省略。
axisbg=color，表示所选子图背景颜色。polar=False，是否为极地投影，默认为否。
projection：使用指定的投影方式，该方式使用之前需要已经注册(registered)。
98.matplotlib.pyplot.subplot2grid(shape, loc, rowspan=1, colspan=1, **k
wargs)
在网格中创建subplot。网格由shape 指定，loc 指定位置，rowspan, colspan 生成行和
列。
99. matplotlib.pyplot.subplot_tool(targetfig=None)
打开图像的subplot 工具窗口。
100.matplotlib.pyplot.subplots(nrows=1, ncols=1, sharex=False, sharey=F
alse, squeeze=True, subplot_kw=None, **fig_kw)
绘制已包含多个子图的图。
nrows : int subplot 网格行数，默认：1
ncols : int subplot 网格列数，默认：1
sharex :
布尔参量或字符串
(“row”, “col”, “all”, or
“none”)
若为True：对所有子图共用x 轴(最后一行除
外),'all'等同于True；‘None’等同于False；若
为‘col’每列子图共享x 轴；若为'row'每行子图
共享x 轴
sharey :
布尔参量或字符串
(“row”, “col”, “all”, or
“none”)
若为True：对所有子图共用y 轴(最后一行除
外),'all'等同于True；‘None’等同于False；若
为‘col’每列子图共享y 轴；若为'row'每行子图
共享y 轴
squeeze : bool
若为True，额外维度被剔除：若为False，没
有改变，返回二维数组
subplot_kw : 字典dict 查询add_subplot()
fig_kw : 字典dict figure()
返回值(fig, ax)均为元组。
101. matplotlib.pyplot.subplots_adjust(*args, **kwargs)
调整subplot 布局。
subplots_adjust(left=None, bottom=None, right=None, top=None,
wspace=None, hspace=None)
各个参数取值0-1.
102. matplotlib.pyplot.suptitle(*args, **kwargs)
为figure 添加总标题。参数均为Text属性，可查询Text。
默认参数：
x : 0.5 文本横坐标
y : 0.98 文本纵坐标
horizontalalignment : ‘center’ 文本的水平对齐方式
verticalalignment : ‘top’ 文本的垂直对齐
103. matplotlib.pyplot.switch_backend(newbackend)
交换默认后台。
104. matplotlib.pyplot.table(**kwargs)
为当前axes 添加table。
table(cellText=None, cellColours=None, cellLoc='right', colWidths=None,
rowLabels=None, rowColours=None, rowLoc='left', colLabels=None,
colColours=None, colLoc='center', loc='bottom', bbox=None):
参数：
agg_filter unknown
alpha 透明度float (取值范围0.0-1.0 )
animated 动画[True | False]
axes an Axes instance
clip_box a matplotlib.transforms.Bbox instance
clip_on [True | False]
clip_path [ (Path, Transform) | Patch | None ]
contains 一个可调用函数
figure
a matplotlib.figure.Figure instance
设置figure 示例
fontsize a float in points
gid 一个id 字符串
label 标签以‘%s’形式显示的字符串及可显示的参数
lod [True | False]
path_effects unknown
picker [None|float|boolean|callable]
rasterized [True | False | None]
sketch_params unknown
snap unknown
transform Transform instance
url a url string
visible [True | False]
zorder any number
105.matplotlib.pyplot.text(x, y, s, fontdict=None, withdash=False, **kwar
gs)
在axes 图轴添加注释。通用参量：
s : 字符串注释内容
x, y : scalars 标量text 注释坐标
fontdict : 字典，可选，默认：无
用于重写默认文本属性。Fontdict=None，缺
省值是由rc 参数确定。
withdash : 布尔参量，可选，默认： 创建一个TextWithDash 实例，而不是Text 实
False 例
其他参量见Text属性。
106. matplotlib.pyplot.thetagrids(*args, **kwargs)
设置极坐标网格θ 位置。
lines, labels = thetagrids()
或：
lines, labels = thetagrids(angles, labels=None, fmt='%d', frac = 1.1)
lines 为Line2D实例，labels 为Text实例
107. matplotlib.pyplot.tick_params(axis='both', **kwargs)
改变刻度及刻度标签的外形。参量：
axis : [‘x’ | ‘y’ | ‘both’] 选择操作的轴线，默认：both
reset : [True | False]
默认False，若为True，所有参
量返回默认状态
which :
[‘major’ | ‘minor’ |
‘both’]
默认为‘major’，选择主次刻度
direction : [‘in’ | ‘out’ | ‘inout’]
刻度放置在axes 内侧、外侧、
两侧都有
length float 刻度线长度
width float 刻度线宽度
color 颜色刻度线颜色
pad float 刻度与标签间距
labelsize
float 或名称，如：
‘large’
刻度标签字体大小
labelcolor 颜色标签颜色
colors 颜色同时指定刻度和标签颜色
zorder int 顺序
bottom, top, left, right : [bool | ‘on’ | ‘off’] 控制各个轴线是否显示刻度
labelbottom, labeltop, labelleft,
labelright
布尔参量或[‘on’ |
‘off’]
控制各个轴线是否显示刻度标
签
108. matplotlib.pyplot.ticklabel_format(**kwargs)
更改标签数据格式。可选参量:
Keyword Description
style
[ ‘sci’ (or ‘scientific’) | ‘plain’
]
‘plain’关闭科学计算法
scilimits (m, n), pair of integers
(m, n)为一对整数。若style=‘sci’，则大于
10 的数采用科学计数法；
useOffset [True | False | offset]
若为True，将根据需要计算偏移量；若为
False，不使用偏移量；也可以手动指定偏移
量
axis [ ‘x’ | ‘y’ | ‘both’ ] 选择轴线
useLocale [True | False]
若为True，数字格式根据当前情况而定；若
为False，采用C 语言格式
109.matplotlib.pyplot.tight_layout(pad=1.08, h_pad=None, w_pad=Non
e, rect=None)
自动调节subplot 参数进行指定填充。参数：
pad : float 填充figure 和子图边缘部分
h_pad, w_pad : float 填充相邻子图边缘部分
rect : (left, bottom, right, top) 指定子图填充区域，默认：(0, 0, 1, 1)
110. matplotlib.pyplot.title(s, *args, **kwargs)
设置当前子图的标题。
label : 字符串标题文本
fontdict : 字典
控制标题外观，如：{‘fontsize’:
rcParams[‘axes.titlesize’],‘verticalalignment’: ‘baseline’,
‘horizontalalignment’: loc}
loc :
{‘center’, ‘left’,
‘right’},字符串，
可选
标题位置，默认center
返回text参量，详情参见text属性。
111. matplotlib.pyplot.tricontour(*args, **kwargs)
在三角形非结构网格绘制等值线。
tricontour(x, y, triangles, mask=mask, ...)
参量：
colors:
[ None | string |
(mpl_colors) ]
若为None，颜色分配（colormap）由
cmap 指定；若为一个色彩字符串，如
colors=‘r’或colors=‘red’，则所以标签显
示这种颜色；若为一系列颜色（色彩字符
串或数组），则不同等值线按顺序绘制不
同色彩
alpha: 0-1 透明度
cmap: [ None | Colormap ]
一个colormap 实例，若cmap=None 且
colors=None，则使用默认默认的colormap
norm: [ None | Normalize ]
一个matplotlib.colors.Normalize 实例，将
data 缩放成色彩，若norm=None 且
colors=None，使用默认线性缩放（linear
scaling）
vmin, vmax: [ None | scalar(标量) ]
若不为None，其他值将被
matplotlib.colors.Normalize 实例支持，根
据levels 覆盖默认缩放色彩
levels: [level0, level1, ..., leveln]
绘制一系列指定值等值线，例如：
levels=[0]只绘制0 等值线
origin:
[ None | ‘upper’ | ‘lower’
| ‘image’ ]
若为None，Z 的第一个值将对应的左下角
(0,0)位置。若为‘image’，将使用
image.origin 对应的rc 值。若x、y 被
contour（）函数调用，该参数不会被激活
extent: [ None | (x0,x1,y0,y1) ] 若origin 为None，extent 值来自
matplotlib.pyplot.imshow()：提供像素坐标
位置，这种情况下，Z[0,0]是像素中心位置，
不是角落；若origin=None，(x0, y0)是
Z[0,0]位置,(x1, y1)是Z[-1,-1]位置。若x、
y 被contour（）函数调用，该参数不会被
激活
locator:
[ None | ticker.Locator
subclass ]
若locator=None，使用默认
MaxNLocator。如果没有指定levels，该参
数用于指定等值线levels
extend:
[ ‘neither’ | ‘both’ | ‘min’
| ‘max’ ]
Unless this is ‘neither’, contour levels are
automatically added to one or both ends of
the range so that all data are included.
These added ranges are then mapped to the
special colormap values which default to
the ends of the colormap range, but can be
set via
matplotlib.colors.Colormap.set_under() and
matplotlib.colors.Colormap.set_over()
methods.
xunits, yunits:
[ None | registered units
]
通过指定
matplotlib.units.ConversionInterface 实例，
覆盖axis 单位
tricontour 函数仅有参量：
linewidths: [None|number|一系列元组或列表]
若为None，使用默认线宽；
若为number，采用统一指
定线宽；若为列表或元组，
不同线宽按列表顺序排列
linestyles:
[ None | ‘solid’ | ‘dashed’ | ‘dashdot’ |
‘dotted’ ]
112. matplotlib.pyplot.tricontourf(*args, **kwargs)
在三角形非结构网格绘制等值线填充图。参量：
colors:
[ None | string |
(mpl_colors) ]
若为None，颜色分配（colormap）由
cmap 指定；若为一个色彩字符串，如
colors=‘r’或colors=‘red’，则所以标签显
示这种颜色；若为一系列颜色（色彩字符
串或数组），则不同等值线按顺序绘制不
同色彩
alpha: 0-1 透明度
cmap: [ None | Colormap ]
一个colormap 实例，若cmap=None 且
colors=None，则使用默认默认的colormap
norm: [ None | Normalize ]
一个matplotlib.colors.Normalize 实例，将
data 缩放成色彩，若norm=None 且
colors=None，使用默认线性缩放（linear
scaling）
vmin, vmax: [ None | scalar(标量) ]
若不为None，其他值将被
matplotlib.colors.Normalize 实例支持，根
据levels 覆盖默认缩放色彩
levels: [level0, level1, ..., leveln]
绘制一系列指定值等值线，例如：
levels=[0]只绘制0 等值线
origin:
[ None | ‘upper’ | ‘lower’
| ‘image’ ]
若为None，Z 的第一个值将对应的左下角
(0,0)位置。若为‘image’，将使用
image.origin 对应的rc 值。若x、y 被
contour（）函数调用，该参数不会被激活
extent: [ None | (x0,x1,y0,y1) ]
若origin 为None，extent 值来自
matplotlib.pyplot.imshow()：提供像素坐标
位置，这种情况下，Z[0,0]是像素中心位置，
不是角落；若origin=None，(x0, y0)是
Z[0,0]位置,(x1, y1)是Z[-1,-1]位置。若x、
y 被contour（）函数调用，该参数不会被
激活
locator:
[ None | ticker.Locator
subclass ]
若locator=None，使用默认
MaxNLocator。如果没有指定levels，该参
数用于指定等值线levels
extend:
[ ‘neither’ | ‘both’ | ‘min’
| ‘max’ ]
Unless this is ‘neither’, contour levels are
automatically added to one or both ends of
the range so that all data are included.
These added ranges are then mapped to the
special colormap values which default to
the ends of the colormap range, but can be
set via
matplotlib.colors.Colormap.set_under() and
matplotlib.colors.Colormap.set_over()
methods.
xunits, yunits:
[ None | registered units
]
通过指定
matplotlib.units.ConversionInterface 实例，
覆盖axis 单位
仅本函数有的重要参量：
nchunk: [ 0 | integer ]
若为0，区域不进行细分。表征填充区域
划分精度
hatches:
一系列交叉填充图案。若为None，没有
hatch 添加到contour。Hatching 仅被
PostScript, PDF, SVG 和Agg backends 支持
113. matplotlib.pyplot.tripcolor(*args, **kwargs)
绘制非结构三角形网格伪彩色图。
tripcolor(x, y, triangles, mask=mask, ...)
具体参数参见pcolor()。
114. matplotlib.pyplot.triplot(*args, **kwargs)
绘制非结构三角形网格点线图。
triplot(x, y, triangles, mask=mask, ...)
具体参数参见plot()。
115. matplotlib.pyplot.twinx(ax=None)
绘制与之前axes 图轴共用x 轴的axes 图，新的axes 图在右侧放置。若ax=None，新的
axes 将覆盖ax，返回ax2 实例。
116. matplotlib.pyplot.twiny(ax=None)
绘制与之前axes 图轴共用y 轴的图，新的axes 图在上方放置。若ax=None，新的
axes 将覆盖ax，返回ax2 实例。
117.matplotlib.pyplot.vlines(x, ymin, ymax, colors='k', linestyles='solid', la
bel='', hold=None, **kwargs)
绘制垂线，类似于hlines()函数(第50 号)。
从每个x 区间的ymin 到ymax 范围绘制水平线
参数：
x : 标量或一维数组指定绘图区间
ymin,
ymax :
标量或一维数组
指定每条线的起始值、终止值，若给定
了标量：所有线长度相同
colors : 形如数组颜色，可选，默认：'k'
linestyles :
[‘solid’ | ‘dashed’ | ‘dashdot’ |
‘dotted’], 可选
label : 字符串，可选，默认: ‘’
其他参数见 LineCollection实例。
118.matplotlib.pyplot.xcorr(x, y, normed=True, detrend, usevlines=True, m
axlags=10,hold=None, **kwargs)
绘制x、y 之间的相关性。
xcorr(self, x, y, normed=True, detrend=mlab.detrend_none,
usevlines=True, maxlags=10, **kwargs)
若normed = True，如果normed = True,通过零滞后自相关规范数据，x 和y 通过
detrend 调用（默认没有归一化）的方式除趋势。x 和y 必须等长。
数据绘制形如plot(lags, c, **kwargs),返回值是元组(lags, c, line)，其中：
$lags 是一个长度为2×maxlags + 1 的滞后（lags）向量
$C 是2×maxlags + 1 的自相关向量
$line 是一个line2d 实例，通过plot()返回
默认linestyle 是None，默认的marker 是'o'，交叉相关性是通过numpy.correlate()函数、
mode = 2 实现的。
若usevlines 是True, vlines()将被调用（而不调用plot()函数），用来绘制从起点到
acorr 的垂线。否则，plot（）由Line2D properties 属性参数（kwargs）决定。
maxlags 是正整数，决定lags 的显示数目。默认值None 将返回（2×len（x）- 1）个
lags，返回值是一个元组(lags, c, linecol, b)，其中：
linecol 是linecollection
b 是x-axis
其他参数参见 Line2D 的kwargs 属性
119. matplotlib.pyplot.xkcd(scale=1, length=100, randomness=2)
matplotlib 模块不包含的字体。
120. matplotlib.pyplot.xlabel(s, *args, **kwargs)
设置x 轴标签
override = {
'fontsize' : 'small',
'verticalalignment' : 'top',
'horizontalalignment' : 'center'
}
详情参见text()函数及属性。
121. matplotlib.pyplot.xlim(*args, **kwargs)
设置当前轴线的取值范围。
xmin, xmax = xlim() # return the current xlim
xlim( (xmin, xmax) ) # set the xlim to xmin, xmax
xlim( xmin, xmax ) # set the xlim to xmin, xmax
122. matplotlib.pyplot.xscale(*args, **kwargs)
设置x 轴的缩放
xscale(scale, **kwargs)
scales 的可选参数：[‘linear’ | ‘log’ | ‘symlog’]
‘linear’
‘log’ basex/basey: x、y 轴对数底数
nonposx/nonposy:
[‘mask’ | ‘clip’ ]：若为‘mask’，屏蔽非正数；若为
‘clip’，将负数转换成接近0 的正数
subsx/subsy:
指定主刻度的标签及刻度，如：[2, 3, 4, 5, 6, 7, 8,
9]
‘symlog’ basex/basey: x、y 轴对数底数
linthreshx/linthreshy:
指定一个区域为线性图，避免在0 附近达到无穷
大
subsx/subsy:
指定主刻度的标签及刻度，如：[2, 3, 4, 5, 6, 7, 8,
9]
linscalex/linscaley: 将线性区域拉伸到对数区域，使用底数为10?
123. matplotlib.pyplot.xticks(*args, **kwargs)
设置刻度及对应标签。xticks(array,'a','b','c'...)其中array 中每个元素与字符串'a','b'...一一对
应，使标签显示为指定字符串。
xticks( arange(5), ('Tom', 'Dick', 'Harry', 'Sally', 'Sue') )
其他参量参见Text属性。
124. matplotlib.pyplot.ylabel(s, *args, **kwargs)
设置y 轴标签。
override = {
'fontsize' : 'small',
'verticalalignment' : 'center',
'horizontalalignment' : 'right',
'rotation'='vertical' : }
详情参见text()函数及属性。
125. matplotlib.pyplot.ylim(*args, **kwargs)
设置当前轴线的取值范围。
ymin, ymax = ylim() # return the current ylim
ylim( (ymin, ymax) ) # set the ylim to ymin, ymax
ylim( ymin, ymax ) # set the ylim to ymin, ymax
126. matplotlib.pyplot.yscale(*args, **kwargs)
设置y 轴的缩放
yscale(scale, **kwargs)
yscales 的可选参数：[‘linear’ | ‘log’ | ‘symlog’]
‘linear’
‘log’ basex/basey: x、y 轴对数底数
nonposx/nonposy:
[‘mask’ | ‘clip’ ]：若为‘mask’，屏蔽非正数；若为
‘clip’，将负数转换成接近0 的正数
subsx/subsy:
指定主刻度的标签及刻度，如：[2, 3, 4, 5, 6, 7, 8,
9]
‘symlog’ basex/basey: x、y 轴对数底数
linthreshx/linthreshy:
指定一个区域为线性图，避免在0 附近达到无穷
大
subsx/subsy:
指定主刻度的标签及刻度，如：[2, 3, 4, 5, 6, 7, 8,
9]
linscalex/linscaley: 将线性区域拉伸到对数区域，使用底数为10?
127. matplotlib.pyplot.yticks(*args, **kwargs)
# return locs, labels where locs is an array of tick locations and
# labels is an array of tick labels.
locs, labels = yticks()
# set the locations of the yticks
yticks( arange(6) )
# set the locations and labels of the yticks
yticks( arange(5), ('Tom', 'Dick', 'Harry', 'Sally', 'Sue') )
相关参数参见Text属性。
128. matplotlib.pyplot.colormaps()
matplotlib 提供一系列彩色表，并可通过register_cmap()函数添加。可通过image, pcolor,
scatter,等函数设置colormap。例如：
imshow(X, cmap=cm.hot)
或者使用set_cmap()函数：
imshow(X)
pyplot.set_cmap('hot')
pyplot.set_cmap('jet')
有几种通用颜色方案：
Sequential 方案对单极性数据，从低到高推进
Diverging 方案双极性数据，从中心向两级通过正负偏差
Cyclic 方案用于绘制，值在端点处环绕，如相位角，风的方向，或一天的时间
Qualitative 方案数据本身没有内在顺序，颜色只是用来区分类别
附录一 Line2D属性的 kwargs：
Property Description
agg_filter unknown
alpha 透明度float (0.0 transparent through 1.0 opaque)
animated 动画[True | False]
antialiased or aa抗锯齿[True | False]
axes an Axes instance
clip_box a matplotlib.transforms.Bbox instance
clip_on [True | False]
clip_path [ (Path, Transform) | Patch | None ]
color or c可选颜色任何matplotlib 颜色
contains 一个可调用函数
dash_capstyle
[设置为虚线的cap（帽子）
样式（linestyles）]
[‘butt’ 一头大| ‘round’ 圆的| ‘projecting’突出的]
dash_joinstyle
[设置虚线的连接方式]
[‘miter’ 斜接| ‘round’ 圆的| ‘bevel’倾斜]
dashes
一列包含[on/off]的列表，表示每个像素点是否显
示
设置虚线序列，通过元组（或列表）的形式，设置
每个像素点是否显示。
drawstyle
[‘default’ | ‘steps’ | ‘steps-pre’ | ‘steps-mid’ | ‘stepspost’]
Default 表示直线，
figure
a matplotlib.figure.Figure instance
设置figure 示例
fillstyle填充方式
[‘full’ | ‘left’ | ‘right’ | ‘bottom’ | ‘top’ | ‘none’]
设置marker 点的填充方式，full 表示全填充，
none 表示不填充，其他为半填充
gid 一个id 字符串（设置artist 属性的id）
label标签以‘%s’形式显示的字符串及可显示的参数
linestyle or ls ['-' | '--' | '-.' | ':' | 'None' | ' ' | '']
linewidth or lw线宽用浮点型数据表示
lod [True | False]
marker点属性见下表maker 属性
markeredgecolor or mec 任何matplotlib 颜色（marker 边缘颜色）
markeredgewidth or mew 用浮点型数据表示（marker 边缘宽度）
markerfacecolor or mfc 任何matplotlib 颜色（marker 内部颜色）
markerfacecoloralt or
mfcalt（marker 交替颜色）
任何matplotlib 颜色（marker 的交替颜色颜色）
markersize or ms 浮点型数据（marker 大小）
markevery None | integer | (startind, stride)
path_effects unknown
picker
float distance in points or callable pick
function fn(artist, event)
pickradius float distance in points
rasterized [True | False | None]
sketch_params unknown
snap unknown
solid_capstyle（为solid 线
的cap[帽子]进行设置）
[‘butt’ 一头大| ‘round’ 圆的| ‘projecting’突出的]
solid_joinstyle[设置solid 线
连接方式]
[‘miter’ 斜接| ‘round’ 圆的| ‘bevel’倾斜]
transform a matplotlib.transforms.Transform instance
url a url string
visible [True | False]
xdata 1D array
ydata 1D array
zorder any number
附录二 matplotlib.patches.Polygon 实例：
Property Description
agg_filter unknown
alpha 透明度float (取值范围0.0-1.0 )
animated 动画[True | False]
antialiased or aa抗锯齿[True | False]
axes an Axes instance
clip_box a matplotlib.transforms.Bbox instance
clip_on [True | False]
clip_path [ (Path, Transform) | Patch | None ]
color or c可选颜色任何matplotlib 颜色
contains 一个可调用函数
edgecolor or ec[边缘颜色]
Matplotlib 色彩, or None 为默认, or ‘none’ 表示无色彩
facecolor or fc Matplotlib 色彩, or None 为默认, or ‘none’ 表示无色彩
figure
a matplotlib.figure.Figure instance
设置figure 示例
fill（填充） [True | False]
gid 一个id 字符串
hatch [‘/’ | ‘\’ | ‘|’ | ‘-‘ | ‘+’ | ‘x’ | ‘o’ | ‘O’ | ‘.’ | ‘*’]
label标签以‘%s’形式显示的字符串及可显示的参数
linestyle or ls [‘solid’ | ‘dashed’ | ‘dashdot’ | ‘dotted’]
linewidth or lw float or None for default
lod [True | False]
path_effects unknown
picker [None|float|boolean|callable]
rasterized [True | False | None]
sketch_params unknown
snap unknown
transform Transform instance
url a url string
visible [True | False]
zorder any number
附录三PolyCollection实例：
Property Description
agg_filter unknown
alpha float (取值范围0.0-1.0 )
animated [True | False]
antialiased or antialiaseds（抗锯
齿）
[True | False]或布尔数组（表示True | False）
array unknown
axes an Axes instance
clim a length 2 sequence of floats
clip_box a matplotlib.transforms.Bbox instance
clip_on [True | False]
clip_path [ (Path, Transform) | Patch | None ]
cmap 一个colormap 或者已注册的colormap 名字
color 任何matplotlib 颜色
contains 一个可调用函数
edgecolor or edgecolors matplotlib 颜色或一系列RGBA 元组
facecolor or facecolors matplotlib 颜色或一系列RGBA 元组
figure a matplotlib.figure.Figure instance
gid 一个ID 字符串
hatch [ ‘/’ | ‘\’ | ‘|’ | ‘-‘ | ‘+’ | ‘x’ | ‘o’ | ‘O’ | ‘.’ | ‘*’ ]
label string or anything printable with ‘%s’ conversion.
linestyle or linestyles or dashes
[‘solid’ | ‘dashed’, ‘dashdot’, ‘dotted’ | (offset, on-off-dashseq)
]
linewidth or lw or linewidths float or sequence of floats
lod [True | False]
norm unknown
Property Description
offset_position unknown
offsets float or sequence of floats
path_effects unknown
picker [None|float|boolean|callable]
pickradius unknown
rasterized [True | False | None]
sketch_params unknown
snap unknown
transform Transform instance
url a url string
urls unknown
visible [True | False]
zorder any number
附录四 matplotlib.collections.BrokenBarHCollection属性：
Property Description
agg_filter unknown
alpha 透明度float (0.0 transparent through 1.0 opaque)
animated 动画[True | False]
antialiased or aa抗锯
齿
[True | False]
array unknown
axes an Axes instance
clim a length 2 sequence of floats
clip_box a matplotlib.transforms.Bbox instance
clip_on [True | False]
clip_path [ (Path, Transform) | Patch | None ]
cmap a colormap or registered colormap name
color or c可选颜色任何matplotlib 颜色
contains 一个可调用函数
edgecolor or
edgecolors
matplotlib color arg or sequence of rgba tuples
facecolor or facecolors matplotlib color arg or sequence of rgba tuples
figure
a matplotlib.figure.Figure instance
设置figure 示例
gid 一个id 字符串（设置artist 属性的id）
hatch [ ‘/’ | ‘\’ | ‘|’ | ‘-‘ | ‘+’ | ‘x’ | ‘o’ | ‘O’ | ‘.’ | ‘*’ ]
label标签以‘%s’形式显示的字符串及可显示的参数
linestyle or ls ['-' | '--' | '-.' | ':' | 'None' | ' ' | '']
linewidth or lw线宽用浮点型数据表示
lod [True | False]
norm unknown
offset_position unknown
offsets float or sequence of floats
path_effects unknown
picker
float distance in points or callable pick
function fn(artist, event)
pickradius float distance in points
rasterized [True | False | None]
sketch_params unknown
snap unknown
transform a matplotlib.transforms.Transform instance
url a url string
urls unknown
visible [True | False]
zorder any number
附录五Text属性：
Property Description
agg_filter unknown
alpha透明度float (0.0-1.0)
animated动画[True | False]
axes an Axes instance
backgroundcolor背景颜
色
any matplotlib color
bbox rectangle prop dict
clip_box a matplotlib.transforms.Bbox instance
clip_on [True | False]
clip_path [ (Path, Transform) | Patch | None ]
color any matplotlib color
contains 一个可调用函数
family or fontfamily or
fontname or name
[FONTNAME | ‘serif’ | ‘sans-serif’ | ‘cursive’ | ‘fantasy’ |
‘monospace’ ]
figure a matplotlib.figure.Figure instance
fontproperties or
font_properties
a matplotlib.font_manager.FontProperties instance
gid an id string
horizontalalignment or ha [ ‘center’ | ‘right’ | ‘left’ ]
label标签string or anything printable with ‘%s’ conversion.
linespacing float (multiple of font size)
lod [True | False]
multialignment [‘left’ | ‘right’ | ‘center’ ]
path_effects unknown
picker [None|float|boolean|callable]
position (x,y)
rasterized [True | False | None]
rotation [ angle in degrees | ‘vertical’ | ‘horizontal’ ]
rotation_mode unknown
size or fontsize
[size in points | ‘xx-small’ | ‘x-small’ | ‘small’ | ‘medium’ | ‘large’ |
‘x-large’ | ‘xx-large’ ]
sketch_params unknown
snap unknown
stretch or fontstretch
[a numeric value in range 0-1000 | ‘ultra-condensed’ | ‘extracondensed’
| ‘condensed’ | ‘semi-condensed’ | ‘normal’ | ‘semiexpanded’
| ‘expanded’ | ‘extra-expanded’ | ‘ultra-expanded’ ]
style or fontstyle [ ‘normal’ | ‘italic’ | ‘oblique’]
text string or anything printable with ‘%s’ conversion.
transform Transform instance
url a url string
variant or fontvariant [ ‘normal’ | ‘small-caps’ ]
verticalalignment or va or
ma
[ ‘center’ | ‘top’ | ‘bottom’ | ‘baseline’ ]
visible [True | False]
weight or fontweight
[a numeric value in range 0-1000 | ‘ultralight’ | ‘light’ | ‘normal’ |
‘regular’ | ‘book’ | ‘medium’ | ‘roman’ | ‘semibold’ | ‘demibold’ |
‘demi’ | ‘bold’ | ‘heavy’ | ‘extra bold’ | ‘black’ ]
x float
y float
zorder any number
附录六Marker 属性：
marker description 描述
”.” point 点
”,” pixel 像素
“o” circle 圈
“v” triangle_down 倒三角形
“^” triangle_up 正三角形
“<” triangle_left 左三角形
“>” triangle_right 又三角形
“1” tri_down tri_down
“2” tri_up tri_up
“3” tri_left tri_left
“4” tri_right tri_right
“8” octagon 八角形
“s” square 正方形
“p” pentagon 五角
“*” star 星星
“h” hexagon1 六角1
“H” hexagon2 六角2
“+” plus 加号
“x” x x 号
“D” diamond 钻石
“d” thin_diamond 细钻
“|” vline v 线
“_” hline H 线
TICKLEFT tickleft 左刻度
TICKRIGHT tickright 右刻度
TICKUP tickup 上刻度
TICKDOWN tickdown 下刻度
CARETLEFT caretleft caretleft
CARETRIGHT caretright caretright
CARETUP caretup caretup
CARETDOWN caretdown caretdown
“None” nothing 无
None nothing 无
” “ nothing 无
“” nothing 无
'$...$'
render the string using
mathtext.
使用mathtext 渲染的字符串。
verts
a list of (x, y) pairs used for
Path vertices.
用于路径顶点（X，Y）对的列表。
path a Path instance. 一个路径实例。
(numsides, style, angle) see below
附录七
datetime 模块的所有类的对象都是不可变的。
datetime.date 类
表示日期，由年、月、日组成。
date 类的实例具有3 个只读属性：year、month、day
date(year, month, day)构造方法
date.today()返回一个表示今天的日期的date 对象。
d.timetuple()返回一个对应于日期d 和时间00:00:00(午夜)的时间元组。
d.weekday()以整数形式返回日期d 是一个星期的第几天，0 表示星期一，6 表示星期天。
d.replace(year=None, month=None, day=None)返回一个新的date 对象。
d.strftime(fmt)格式化日期对象，返回字符串。
datetime.time 类
time 类的实例表示一天中的时间。
time(hour=0, minute=0, second=0, microsecond=0, tzinfo=None)构造方法。
t.replace(hour=None, minute=None, second=None, microsecond=None[, tzinfo])返回一个
新的time 对象。
t.strftime(fmt)格式化时间对象，返回字符串。
datetime.datetime 类
datetime 类的实力表示一个时刻（日期和这个日期的特定时间）。datetime 类是date 类
的子类，并添加了time 的属性。
datetime(year, month, day, hour=0, minute=0, second=0, microsecond=0, tzinfo=None)构
造方法。
datetime.now(tz=None)返回一个表示当前本地日期和时间的datetime 对象。
datetime.today()返回一个表示当前本地日期和时间的datetime 对象。
d.date()返回一个表示与d 对象的日期相同的date 对象。
d.time()返回一个表示与d 对象的时间相同的time 对象。
d.replace(year=None, month=None, day=None, hour=None, minute=None,
second=None, microsecond=None[, tzinfo])返回一个新的datetime 对象。
d.weekday()
d.strftime(fmt)格式化，返回字符串。
datetime.strptime(str, fmt)将字符串str 按照格式fmt，返回datetime 对象。
d.timetuple()返回一个对应时刻d 的时间元组。
datetime.conbine(date, time)根据date 对象和time 对象，创建datetime 对象。
datetime.timedelta 类
表示时间间隔，使用3 个只读属性: days、seconds 和microseconds
timedelta(days=0, seconds=0, misroseconds=0, milliseconds=0, minutes=0, hours=0,
weeks=0)构造方法
从一个timedelta 对象计算对应的时间间隔，单位为秒： t.days*86400 + d.seconds +
milliseconds/1000000.0
date 对象和datetime 对象可与timedelta 对象进行加减运算：
date2 = date1 +timedelta #日期对象加上时间间隔，返回新日期对象
date 对象之间、datetime 对象之间、time 对象可进行比较（>~<）。
格式字符 意义
%a 星期的简写。如 星期三为Web
%A 星期的全写。如 星期三为Wednesday
%b 月份的简写。如4 月份为Apr
%B 月份的全写。如4 月份为April
%c: 日期时间的字符串表示。（如： 04/07/10 10:43:39）
%d: 日在这个月中的天数（是这个月的第几天）
%f: 微秒（范围[0,999999]）
%H: 小时（24 小时制，[0, 23]）
%I: 小时（12 小时制，[0, 11]）
%j: 日在年中的天数 [001,366]（是当年的第几天）
%m: 月份（[01,12]）
%M: 分钟（[00,59]）
%p: AM 或者PM
%S: 秒（范围为[00,61]，为什么不是[00, 59]，参考python 手册~_~）
%U: 周在当年的周数当年的第几周），星期天作为周的第一天
%w: 今天在这周的天数，范围为[0, 6]，6 表示星期天
%W: 周在当年的周数（是当年的第几周），星期一作为周的第一天
%x: 日期字符串（如：04/07/10）
%X: 时间字符串（如：10:43:39）
%y: 2 个数字表示的年份
%Y: 4 个数字表示的年份
%z: 与utc 时间的间隔 （如果是本地时间，返回空字符串）
%Z: 时区名称（如果是本地时间，返回空字符串）
%%: %% => %